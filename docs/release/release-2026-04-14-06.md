## Highlights

- Closes the remaining Architecture 09 coding-server maturity-summary drift by removing the stale “minimal `/health` host” narrative from the active install/deploy/release standard.
- Extends `scripts/live-docs-governance-baseline.test.mjs` so Architecture 09 must now stay aligned with the already-closed representative `core / app / admin` routes, canonical OpenAPI release evidence, and PostgreSQL host-pass truth.
- Backwrites the Step 09 historical outcome, loop-control prompt, and release registry so future autonomous loops do not reopen this earlier placeholder summary without fresh failing evidence.

## Scope

- `scripts/live-docs-governance-baseline.test.mjs`
- `docs/架构/09-安装-部署-发布标准.md`
- `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/release/release-2026-04-14-06.md`
- `docs/release/releases.json`

## Verification

- `node scripts/live-docs-governance-baseline.test.mjs`
- `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`
- `node scripts/check-release-closure.mjs`

## Notes

- This release does not reopen Step `09`, Step `17`, or Step `18` implementation lanes; it only realigns the active Architecture 09 maturity summary to the later route, OpenAPI, and PostgreSQL closures already recorded elsewhere in the repo.
- The root cause was a stale top-of-page maturity summary in Architecture 09 that predated the later representative-route and OpenAPI release-governance closures, even though those later closures were already frozen in live docs and release notes.

## Post-release operations

- Observation goal: keep Architecture 09 aligned with the later coding-server route, OpenAPI, and PostgreSQL host-pass truth instead of drifting back to the earlier minimal-host placeholder narrative.
- Observation window: on `2026-04-14`, rerun `node scripts/live-docs-governance-baseline.test.mjs`, `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`, and `node scripts/check-release-closure.mjs` after any edit to `docs/架构/09-安装-部署-发布标准.md`, `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`, `docs/prompts/反复执行Step指令.md`, or `scripts/live-docs-governance-baseline.test.mjs`.
- Stop-ship signals: Architecture 09 reintroduces the minimal `/health` Rust host summary; it again treats representative `core / app / admin` or canonical OpenAPI release evidence as future work; Step 09 stops carrying the historical closure addendum; or `check-release-closure` no longer points at this latest registry-backed note.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-14-06 --release-assets-dir artifacts/release`.
- Re-issue path: restore Architecture 09 and Step 09 to the governed host-pass maturity truth -> rerun `node scripts/live-docs-governance-baseline.test.mjs` -> rerun `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs` -> rerun `node scripts/check-release-closure.mjs`.
- Writeback targets: `scripts/live-docs-governance-baseline.test.mjs`, `docs/架构/09-安装-部署-发布标准.md`, `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`, `docs/prompts/反复执行Step指令.md`, `docs/release/release-2026-04-14-06.md`, and `docs/release/releases.json`.
