## Highlights

- Centralizes defensive `projectId` and `runConfigurationId` fallback into shared viewer helpers: `normalizeEvidenceProjectId(...)` and `normalizeEvidenceRunConfigurationId(...)`.
- Removes scattered inline `global/default` scope fallback from template, summary, diagnostics, compare, and export helper paths inside `viewer.ts`.
- Preserves the stricter normalized-entry contract already enforced by `UnifiedStudioEvidenceEntry`, while keeping helper-level resilience for malformed ad-hoc inputs.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration mirrors the defensive profile-helper pattern already established in `release-2026-04-08-63`, but for scope fields.
- `global/default` remains the only defensive fallback target for malformed helper input that bypasses unified entry assembly.
- The next bounded Step 07 slice can continue tightening remaining helper duplication only where behavior is already normalized and verified.
