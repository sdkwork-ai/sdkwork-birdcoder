## Highlights

- Publishes the first GitHub-backed BirdCoder release from the current `main` branch after confirming that no earlier GitHub Release or release tag exists for this repository.
- Carries forward the unpublished release chain through `release-2026-04-28-01`, including the pending history from earlier release notes.
- Hardens BirdCoder runtime activity surfaces across Code, Studio, Shell, Settings, Identity, Terminal, project/session inventory, transcript loading, file search, provider-backed persistence, and release governance.
- Preserves the multi-window programming standard while syncing the lockfile importer for the current workspace package layout.
- Remediates the active problem list by tightening provider adapters, official SDK runtime selection, CLI fallback contracts, user storage, draft persistence, terminal runtime handling, and problem-list governance.
- Removes retired compatibility surfaces including the retired VIP route option, Codex compatibility debt, and the unused Tauri dialog permission path.
- Adds a browser QRCode alias shim and contract so Vite development and production resolution use the browser-safe QRCode entry consistently.
- Switches GitHub Actions shared SDK materialization to passwordless SSH for private sibling repositories.
- Aligns CI and reusable release jobs with the SDKWork baseline by exposing checked-out POSIX `sdkwork-run-node` and `sdkwork-run-pnpm` command wrappers.
- Fixes multiple GitHub Actions release verification failures by resolving shared SDK sources, aliases, React compatibility, Rust toolchain PATH, Tauri SQLite path resolution, and terminal dependencies.
- Hardens the release parity contract so BirdCoder compares release workflow shape against the committed SDKWork baseline.
- Follows the published SDKWork release SDK build baseline by pinning BirdCoder CI Rust setup to `dtolnay/rust-toolchain@1.90.0`.

## Scope

Extensive scope covering CI workflows, release scripts, Docker deployment, SDK materialization, Rust toolchain, Tauri CLI, engine contracts, and quality gates.

## Verification

- `pnpm check:ci-flow`
- `pnpm check:release-flow`
- `pnpm check:governance-regression`
- `pnpm check:quality:release`
- Local release rehearsal flow

## Notes

- The release chain carries forward unpublished history from earlier release notes through the first GitHub Release.
- CI/release workflow wrappers are now portable across Linux, macOS, and Windows runners.
- Shared SDK git-mode dependencies are materialized over SSH during release.
- This canary remains blocked for formal or general-availability promotion until desktop installer signing and trust evidence is finalized.

## Post-release operations

- Observation goal: keep the first GitHub-backed canary release chain, SSH SDK materialization, CI wrapper portability, and desktop trust evidence aligned before formal or GA promotion.
- Observation window: after any edit to `.github/workflows/package.yml`, `scripts/release/`, desktop signing or notarization lanes, or release registry rendering, rerun the verification commands above and inspect release rehearsal artifacts.
- Stop-ship signals: desktop installer signing or trust evidence remains pending; GitHub Release packaging diverges from governed release rehearsal output; or shared SDK materialization over SSH fails on CI runners.
- Release kind: `canary`
- Rollout stage: `ring-1`
- Formal or GA status: `blocked`
- Machine stop-ship signals: pending desktop installer signing/trust evidence blocks formal or general-availability promotion
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-05-06-01 --release-assets-dir artifacts/release`
- Re-issue path: restore governed release packaging -> refresh desktop trust evidence -> rerun `pnpm check:release-flow`, `pnpm check:quality:release`, and `pnpm lint` -> update release registry and notes together.
- Writeback targets: `.github/workflows/package.yml`, `scripts/release/`, `scripts/check-release-closure.mjs`, `docs/core/release-and-deployment.md`, `docs/release/release-2026-05-06-01.md`, and `docs/release/releases.json`.
