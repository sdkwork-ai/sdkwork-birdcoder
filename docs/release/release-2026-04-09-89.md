## Highlights

- Extends the Step 10 governance regression report into explicit Claw-compatible server build and desktop release build entrypoint contracts.
- Reuses the existing `run-claw-server-build.test.mjs` and `run-desktop-release-build.test.mjs` checks inside `scripts/governance-regression-report.mjs` instead of leaving build-entry validation hidden inside `check:release-flow` only.
- Raises the shared JSON governance regression artifact from 56 to 58 checks while bringing desktop and server build-entry orchestration into the same repeatable report as runtime, delivery, browser, workspace-quality, release-asset, release-note, smoke, and architecture governance.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next Step 10 build-orchestration slice by making BirdCoder's server build wrapper and desktop release build planner visible in the machine-readable governance artifact instead of only the release-flow aggregate.
- The report stays contract-first: it widens by importing existing build-entry contracts instead of widening the report schema.
- The next best expansion remains another uncovered operational contract family with direct delivery risk, not duplicate reporting surfaces.
