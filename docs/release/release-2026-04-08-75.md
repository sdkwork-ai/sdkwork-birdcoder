## Highlights

- Extends the Step 10 governance regression lane into the real Studio execution family.
- Reuses the existing Studio Preview / Build / Test / Simulator execution contracts inside the shared governance regression report instead of introducing a separate Studio regression pipeline.
- Keeps `artifacts/governance/governance-regression-report.json` as the single repeatable JSON surface while widening coverage from hosts and release gates into the actual Studio tool lanes.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/step/10-性能-安全-观测-审计治理.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This closes the first Studio execution expansion slice of `CP10-3`; the shared governance regression report now covers Preview / Build / Test / Simulator execution in the same repeatable artifact.
- The report still stays contract-first: it widens by reusing existing Studio execution contracts, not by creating a second Studio-only regression stack.
- The next optimal expansion is deeper coverage of remaining tool families or evidence/viewer contracts, not more report shapes.
