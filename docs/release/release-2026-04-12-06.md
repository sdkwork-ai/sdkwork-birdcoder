## Highlights

- Re-runs PostgreSQL live smoke and records the real result as `blocked` with `reasonCode=missing_postgresql_dsn` instead of fabricating DSN-backed closure.
- Closes the next non-environmental Step 17D slice by promoting `test:postgresql-live-smoke-contract` into `lint`, `check:release-flow`, and governance regression aggregation.
- Realigns governance writeback and docs baseline to `87` executable checks.

## Scope

- `package.json`
- `scripts/release-flow-contract.test.mjs`
- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/架构/20-统一Rust-Coding-Server-API-协议标准.md`
- `docs/step/17D-PostgreSQL-Live-Smoke-Preflight.md`
- `docs/step/17-Coding-Server-Core-App-Admin-API与控制台实现.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/release/release-2026-04-12-06.md`
- `docs/release/releases.json`

## Verification

- `pnpm.cmd run release:smoke:postgresql-live` -> `blocked (reasonCode=missing_postgresql_dsn)`
- `pnpm.cmd run test:postgresql-live-smoke-contract`
- `node scripts/release-flow-contract.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `node scripts/live-docs-governance-baseline.test.mjs`
- `pnpm.cmd run typecheck`
- `pnpm.cmd run docs:build`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd run check:governance-regression` -> `passed (87/87)`
- `pnpm.cmd run check:release-flow`

## Notes

- This release closes governance hardening for PostgreSQL live-smoke preflight, not DSN environment provisioning.
- Step 17 still has one serial environment gate: a real DSN-backed `pnpm.cmd run release:smoke:postgresql-live` run must return `passed` before PostgreSQL closure can be claimed.
- When the smoke result remains `missing_postgresql_dsn` or `missing_postgresql_driver`, keep it explicit and continue only on non-environmental slices.

## Post-release operations

- Observation goal: keep PostgreSQL live-smoke governance promotion stable across lint, release-flow, and governance regression while preserving explicit blocked-state evidence.
- Observation window: monitor the next Step loop on `2026-04-12` with `check:governance-regression` and `check:release-flow` as release-blocking gates.
- Stop-ship signals: `test:postgresql-live-smoke-contract` missing from any promoted gate, governance baseline count drift, or release-closure verification failure.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-12-06 --release-assets-dir artifacts/release`.
- Re-issue path: `pnpm release:plan` -> restore PostgreSQL live-smoke governance promotion and docs writeback -> rerun verification gates -> `pnpm release:finalize`.
- Writeback targets: `docs/release/release-2026-04-12-06.md`, `docs/release/releases.json`, `docs/prompts/反复执行Step指令.md`, Step 17 docs, and architecture/release-core docs.
