## Highlights

- Tightens `UnifiedStudioEvidenceEntry` so `projectId` and `runConfigurationId` are required string fields instead of nullable scope placeholders.
- Updates `StudioEvidencePanel` to consume normalized project and run-configuration scope directly, removing redundant UI-level `global/default` fallback logic.
- Keeps the change bounded to Step 07 scope-contract alignment after unified entry normalization was already introduced in the previous slice.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration does not change storage shape or export schema; it only tightens the in-memory unified entry contract and a primary UI consumer.
- `global/default` remains the normalized scope baseline, but it is now expected to be present before the panel receives the unified entry.
- The next bounded slice can continue removing redundant fallback logic from other Step 07 consumers now that the normalized scope contract is explicit.
