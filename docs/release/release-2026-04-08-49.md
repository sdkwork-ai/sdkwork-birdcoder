## Highlights

- Extends the compare markdown family with shared `Entry A/B Project` scope lines so pairwise triage outputs no longer require a detail-section or notes jump to recover project context.
- Reuses a small compare-project helper in `viewer.ts` across compare template, compare issue, compare summary, and compare release-note builders.
- Keeps the iteration inside the Step 07 viewer contract and docs without widening UI, storage, or release schemas.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-issue-template补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-summary-template补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-release-note-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration tightens pairwise scope context, not UI behavior; the same compare copy actions now surface project identity earlier in the markdown output.
- The release-note builder still keeps the existing notes section, so this slice improves scope visibility without changing the broader compare output structure.
- The next bounded slice can remove remaining compare-family duplication by extracting one shared scope-line builder after the new project context is fully locked by tests.
