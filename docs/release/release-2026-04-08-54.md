## Highlights

- Extends the compare markdown family with shared `Entry A/B Lane` scope lines so pairwise triage outputs expose concrete lane context instead of only the `Same Lane` delta.
- Consolidates compare scope assembly into one shared builder inside `viewer.ts`, reducing duplication across compare template, issue, summary, and release-note builders.
- Keeps the iteration inside the Step 07 viewer contract and docs without widening UI, storage, or release schemas.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-identity-block补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-issue-template补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-summary-template补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-release-note-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration adds lane identity to the top-level compare scope and uses that change to collapse repeated scope assembly into one shared builder.
- The external compare copy actions remain the same; only the standardized markdown scope block becomes richer and easier to keep aligned.
- The next bounded slice can continue trimming compare-family duplication by standardizing section transitions and blank-line formatting where it still differs.
