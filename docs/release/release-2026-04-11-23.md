## Highlights

- Closes the fifth Step 18 non-environmental slice by promoting the existing engine runtime adapter, conformance, tool protocol, and resume or recovery checks into `check:release-flow`.
- Makes Step 18 engine-adapter governance release-blocking instead of leaving it on optional root commands.
- Reconfirms that PostgreSQL live smoke is still environment-blocked with `reasonCode=missing_postgresql_dsn`.

## Scope

- `scripts/release-flow-contract.test.mjs`
- `package.json`
- `docs/prompts/反复执行Step指令.md`
- `docs/step/18E-Engine-Governance-Release-Flow-Promotion-Lane.md`
- `docs/架构/26-Step-18-Engine-Governance-Release-Flow-Standard.md`
- `docs/release/releases.json`

## Verification

- `pnpm.cmd run release:smoke:postgresql-live`
- `pnpm.cmd run test:engine-runtime-adapter`
- `pnpm.cmd run test:engine-conformance`
- `pnpm.cmd run test:tool-protocol-contract`
- `pnpm.cmd run test:engine-resume-recovery-contract`
- `node scripts/release-flow-contract.test.mjs`
- `pnpm.cmd run docs:build`
- `pnpm.cmd run check:release-flow`

## Post-release operations

- Observation window: `0` minutes on `pending`.
- Stop-ship signals: any Step 18 governance command dropping out of `check:release-flow`, `release-flow-contract.test.mjs` no longer enforcing that membership, or any of the four promoted Step 18 commands regressing under release-flow.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-11-23 --release-assets-dir artifacts/release`.
- Re-issue path: `pnpm release:plan` -> restore Step 18 release-flow membership -> rerun engine governance quartet -> docs/release backwrite -> `pnpm release:finalize`.
- Writeback targets: `scripts/release-flow-contract.test.mjs`, `package.json`, `docs/prompts/反复执行Step指令.md`, `docs/step/18E-Engine-Governance-Release-Flow-Promotion-Lane.md`, `docs/架构/26-Step-18-Engine-Governance-Release-Flow-Standard.md`, `docs/release/releases.json`, and `docs/release/release-2026-04-11-23.md`.

## Notes

- This release closes Step 18 release-flow promotion only; it does not yet surface the same governance quartet inside score-bearing governance regression or quality-matrix outputs.
- PostgreSQL live smoke remains blocked when no real DSN is configured; this release does not claim PostgreSQL closure.
- The next non-environmental slice is making the Step 18 governance quartet visible in governance regression and quality scoring outputs.
