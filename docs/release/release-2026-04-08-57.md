## Highlights

- Extends export normalization so `summary.projectIds` and exported entry `projectId/runConfigurationId` values also fall back to `global/default`.
- Keeps the change inside `viewer.ts` and the Step 07 contract docs without widening UI, storage, or release schemas.
- Aligns raw export payloads with the same scope defaults already used by diagnostics and markdown triage outputs.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration standardizes export scope fallback behavior; it does not change UI actions or release-flow wiring.
- The normalized defaults are `global` for project scope and `default` for run configuration scope, matching the existing diagnostics and compare contracts.
- The next bounded slice can continue with any remaining export-entry normalization gaps or return to compare-family formatting cleanup.
