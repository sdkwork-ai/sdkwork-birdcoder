## Highlights

- Aligns the compare markdown family on one shared `Evidence A/B` identity block so pairwise triage outputs no longer drift on top-level pair identity.
- Introduces a small shared compare-identity helper inside the Step 07 viewer contract and reuses it across compare template, compare issue, compare summary, and compare release note builders.
- Keeps the iteration fully inside `viewer.ts` and contract docs without widening UI, storage, or release schemas.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-identity-block补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This is a contract-alignment iteration: copied outputs stay behaviorally the same except for the newly standardized top-level pair identity lines.
- The change starts reducing duplication in the compare builder family without forcing a larger refactor in the same turn.
- The next bounded slice can continue that refactor by extracting shared visible-slice and compare scope builders under the same verified contract.
