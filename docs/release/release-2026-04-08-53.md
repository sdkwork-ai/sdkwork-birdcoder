## Highlights

- Normalizes compare release-note `Notes` by keeping a stable `Source: Studio Evidence Viewer` line after scope-field deduplication.
- Keeps the change inside `viewer.ts` and the Step 07 release-note contract docs without widening UI, storage, or release schemas.
- Preserves the same compare copy action while avoiding an empty notes section in the generated release-note template.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-release-note-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This is a contract-tightening iteration: compare release-note output now keeps a stable non-empty `Notes` block after scope-field deduplication.
- The change adds one normalized notes line and preserves the same top-level scope, verification, and compare-difference sections.
- The next bounded slice can extract a shared compare scope builder and keep shrinking duplicated markdown assembly inside `viewer.ts`.
