## Highlights

- Adds `Copy Compare Summary` to the unified Studio evidence viewer so two selected Step 07 evidence records can move into lightweight markdown triage without carrying full compare payloads.
- Introduces a shared comparison-summary builder that reuses the existing pairwise compare dimensions and selected evidence identities.
- Keeps the iteration inside the Step 07 compare/clipboard lane without widening storage, release schemas, or renderer contracts.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-summary-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- The comparison summary intentionally stays lighter than `Copy Compare Template`; it preserves pairwise scope and compare lines without embedding full per-entry details.
- This closes the symmetry gap between visible-slice summary copy and the existing compare copy family.
- The next bounded Step 07 slice can either consolidate the growing builder layer or extend compare outputs into stricter issue/release automation hooks.
