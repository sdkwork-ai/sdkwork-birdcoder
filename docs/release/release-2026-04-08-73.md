## Highlights

- Extends the Step 10 governance regression lane from Terminal/Web/Release into the host runtime dimension.
- Reuses the existing `host-runtime` contract inside the shared governance regression report instead of creating a separate host-only report.
- Keeps `artifacts/governance/governance-regression-report.json` as the single repeatable JSON surface while widening coverage to desktop/server runtime identity checks.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/step/10-性能-安全-观测-审计治理.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This closes the first host-runtime expansion slice of `CP10-3`; the shared governance regression report now covers host runtime parity in addition to governance, terminal, web budget, and release closure gates.
- The report still stays contract-first: it widens by reusing an existing host-runtime gate, not by inventing a second host regression model.
- The next optimal expansion is more host/tool coverage, especially host-studio or other governed tool families.
