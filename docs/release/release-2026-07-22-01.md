# SDKWork BirdCoder Pre-Launch Release State

Status: blocked
Updated: 2026-07-23
Release kind: `pre-launch`
Rollout stage: `not-started`
Formal or GA status: `blocked`
Machine stop-ship signals: `signed-production-artifact-evidence-missing`

## Candidate Scope

- Stateless Rust gateway with four BirdCoder System App API reads.
- PC browser/Tauri workbench using canonical owner SDKs.
- Agents-owned Project, composition, Session, Session Item, and Runtime Binding
  workflows.
- PC/Tauri local filesystem, Git, worktree, terminal, and allowlisted device
  state.

H5 and Flutter are not part of this candidate's migration evidence.

## Stop-Ship Conditions

Publication remains blocked while any Rust, PC typecheck/lint, API, SDK, IAM,
architecture, security, documentation, package, signing, checksum, SBOM,
attestation, smoke, rollback, or reverse-scan gate is incomplete.

Synthetic fixtures and dry-run artifacts are rehearsal evidence only. They
cannot enable an install package or change pre-launch status.

## Rollback

For a failed release candidate, deploy the previous immutable artifact and
matching configuration or stop promotion. BirdCoder has no server business
database to restore. Owner-domain recovery remains with Agents, Skills, IAM,
IM, and the other affected module.

## Promotion

Promotion requires a finalized immutable release manifest, checksums,
attestations, required signatures, SBOM, target smoke evidence, an approved
rollback plan, and all current architecture gates. Update the machine release
registry and this note only from verified release evidence.

## Post-release operations

- Observation window: monitor health, readiness, authorization failures, dependency diagnostics, and the approved error budget for the release plan's full observation period.
- Stop-ship signals: keep promotion blocked for any machine registry signal, trust failure, architecture regression, or unresolved smoke failure.
- Rollback entry: record the prior immutable artifact and compatible configuration before promotion.
- Re-issue path: rebuild from the approved commit and issue new immutable evidence; never mutate a finalized artifact.
- Writeback targets: update the release registry, this note, artifact inventory, and deployment record only from verified final evidence.
