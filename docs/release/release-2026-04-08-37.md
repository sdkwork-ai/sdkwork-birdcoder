## Highlights

- Adds `Copy Visible Diagnostics` to the unified Studio evidence viewer so the currently filtered slice can move into clipboard-based triage without downloading a file.
- Reuses the existing export bundle contract for clipboard copy, keeping visible-slice copy and JSON export on the same Step 07 payload shape.
- Keeps the change fully local to the viewer/page interaction layer, with no release schema changes.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- Visible-slice clipboard copy intentionally reuses the export bundle so downloaded JSON and copied JSON stay structurally identical.
- This iteration improves operator workflow rather than widening the evidence schema.
- The next Step 07 slice can focus on batch comparison or issue-template autofill.
