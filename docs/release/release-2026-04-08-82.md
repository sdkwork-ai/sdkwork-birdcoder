## Highlights

- Extends the Step 10 governance regression report into the shared run-configuration storage and normalization contract.
- Reuses the existing `run-config` contract inside `scripts/governance-regression-report.mjs` instead of introducing a second run-config governance lane.
- Raises the shared JSON governance regression artifact from 20 to 21 checks while keeping the same `artifacts/governance/governance-regression-report.json` output contract.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next `CP10-3` runtime-surface slice by proving that run-configuration defaults, normalization, and directory resolution live inside the same repeatable governance aggregation path as launch requests.
- The report stays contract-first: it widens by importing an existing run-configuration contract instead of widening the report schema.
- The next best expansion remains the next uncovered governed runtime or release contract, not more aggregation plumbing.
