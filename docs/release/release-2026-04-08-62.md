## Highlights

- Tightens the Step 07 unified evidence profile contract so `StudioEvidencePanel` consumes normalized `profileId` directly instead of reapplying `powershell` fallback at render time.
- Keeps profile consumption aligned with the same normalized-entry model already adopted for `projectId` and `runConfigurationId`.
- Preserves the existing `powershell` default at unified entry assembly while removing redundant UI-level fallback logic.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration changes only panel consumption and contract documentation; it does not modify storage shape, replay behavior, or export schema.
- `powershell` remains the normalized profile baseline, but it is now expected to be present before the panel receives the unified entry.
- The next bounded slice can continue removing redundant fallback logic from other Step 07 consumers now that panel scope and profile consumption are symmetric.
