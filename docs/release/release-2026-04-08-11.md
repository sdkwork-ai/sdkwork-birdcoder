## Highlights

- Adds a shared blocked-launch action contract for missing AI CLI profiles instead of leaving Settings guidance as page-local copy.
- Updates TerminalPage to consume that shared action and automatically route blocked Codex, Claude Code, Gemini, and OpenCode launches toward Settings-based environment configuration.
- Keeps the terminal launch contract aligned across state evaluation, presentation, dead-code prevention, and next-step guidance.
- Extends Step 08 documentation so missing-CLI recovery is now described as a first-class action, not just a message.

## Scope

- `packages/sdkwork-birdcoder-commons/src/terminal/runtime.ts`
- `packages/sdkwork-birdcoder-terminal/src/pages/TerminalPage.tsx`
- `scripts/terminal-cli-registry-contract.test.ts`
- `docs/架构/16-终端主机会话-运行配置-本地存储标准.md`
- `docs/step/08-terminal主机会话-cli集成-sqlite标准化.md`
- `docs/release/releases.json`

## Verification

- `node scripts/terminal-cli-registry-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd build`
- `pnpm.cmd docs:build`
- `pnpm.cmd check:multi-mode`

## Notes

- This iteration keeps the recovery action intentionally narrow: missing CLI launches route to Settings, but no install-button or provider-specific setup wizard is added yet.
- Carry forward remains propagating the same blocked-launch guidance into Code/Studio terminal entry points and adding richer install/config affordances per engine.
