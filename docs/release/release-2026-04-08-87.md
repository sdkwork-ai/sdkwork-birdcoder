## Highlights

- Extends the Step 10 governance regression report into shared desktop Tauri dev, source parse, Tailwind source, Studio layout stability, architecture boundary, and BirdCoder structure contracts.
- Reuses the existing `desktop-tauri-dev`, `source-parse`, `tailwind-source`, `studio-chat-layout`, `studio-sidebar-stability`, `arch-boundaries`, and `sdkwork-birdcoder-structure` contracts inside `scripts/governance-regression-report.mjs` instead of introducing a second workspace-quality governance report.
- Raises the shared JSON governance regression artifact from 34 to 41 checks while bringing the same unified report across runtime, delivery, browser, and workspace-quality governance surfaces.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next `CP10-3` workspace-quality slice by proving that desktop dev boot shape, TS/TSX parse integrity, Tailwind source reachability, Studio layout stability, architecture dependency boundaries, and repository structure compliance live inside the same repeatable governance aggregation path as runtime and delivery governance.
- The report stays contract-first: it widens by importing existing workspace-quality contracts instead of widening the report schema.
- The next best expansion remains another uncovered contract family with clear operational impact, not more report plumbing.
