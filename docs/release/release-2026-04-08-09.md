## Highlights

- Continues Step 08 terminal polish by pushing launch detail text toward the shared launch-presentation model instead of relying on page-local explanatory strings.
- Strengthens blocked AI CLI guidance so launch interception now points users to Settings-based environment configuration instead of only showing a generic unavailable message.
- Keeps dropdown items and recent-session items aligned around the same launch-state semantics: runnable, install-required, or browser-unknown.
- Updates the architecture and Step documentation so the terminal UX rule is explicit, testable, and release-traceable.

## Scope

- `packages/sdkwork-birdcoder-terminal/src/pages/TerminalPage.tsx`
- `packages/sdkwork-birdcoder-commons/src/terminal/runtime.ts`
- `docs/架构/16-终端主机会话-运行配置-本地存储标准.md`
- `docs/step/08-terminal主机会话-cli集成-sqlite标准化.md`
- `docs/release/releases.json`

## Verification

- `node scripts/terminal-cli-registry-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd build`
- `pnpm.cmd docs:build`
- `pnpm.cmd check:multi-mode`

## Notes

- Terminal still contains one hidden legacy detail fragment in the dropdown render path; active UX already reads from the shared launch-presentation model, but that dead fragment remains a cleanup item.
- Carry forward remains removing the final dead JSX branch, adding richer install actions, and extending the same install guidance to broader Code/Studio terminal entry points.
