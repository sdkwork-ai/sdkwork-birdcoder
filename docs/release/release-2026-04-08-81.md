## Highlights

- Extends the Step 10 governance regression report into guarded run-configuration terminal launch requests.
- Reuses the existing `run-config-request` contract inside `scripts/governance-regression-report.mjs` instead of creating a second run-config-only regression pipeline.
- Raises the shared JSON regression artifact from 19 to 20 checks while keeping the same `artifacts/governance/governance-regression-report.json` output contract.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next `CP10-3` request-surface slice by proving that run-configuration launch requests obey the same repeatable governance aggregation path as CLI registry recovery.
- The report stays contract-first: it widens by importing an existing terminal launch-request contract instead of introducing a new governance report shape.
- The next best expansion is the next remaining governed runtime/request contract, not more report plumbing.
