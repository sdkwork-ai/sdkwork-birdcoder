## Highlights

- Extends the Step 10 governance regression report into the shared Terminal runtime, session, and host-runtime contracts.
- Reuses the existing `terminal-runtime`, `terminal-session`, and `terminal-host-runtime` contracts inside `scripts/governance-regression-report.mjs` instead of introducing a separate terminal-runtime governance report.
- Raises the shared JSON governance regression artifact from 21 to 24 checks while keeping the same `artifacts/governance/governance-regression-report.json` output contract.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next `CP10-3` terminal-runtime slice by proving that execution-plan assembly, session normalization, and host output normalization live inside the same repeatable governance aggregation path as launch request and run-configuration governance.
- The report stays contract-first: it widens by importing existing terminal runtime contracts instead of widening the report schema.
- The next best expansion remains another uncovered runtime, persistence, or workbench contract with direct operational impact.
