## Highlights

- Deduplicates compare release-note scope by removing repeated `Entry A/B Project` lines from `Notes` once project context is standardized in the top-level `Scope` block.
- Keeps the change inside `viewer.ts` and the Step 07 markdown contract docs without widening UI, storage, or release schemas.
- Tightens the compare release-note output so pairwise triage templates stay concise while preserving the same compare actions and gating flow.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-identity-block补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-release-note-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This is a contract-tightening iteration: compare release-note templates now keep project scope in one place instead of repeating it in `Notes`.
- The change reduces output noise but preserves the same top-level compare scope fields and the same clipboard wiring.
- The next bounded slice can extract one shared compare scope builder and use it to reduce the remaining compare-family duplication in `viewer.ts`.
