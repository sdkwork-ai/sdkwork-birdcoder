## Highlights

- Extends the Step 10 governance regression lane into the Studio simulator UI family.
- Reuses the existing `studio-simulator-ui` contract inside the shared governance regression report instead of creating a separate simulator-UI regression pipeline.
- Keeps `artifacts/governance/governance-regression-report.json` as the single repeatable JSON surface while widening coverage from execution, evidence persistence, and evidence viewing into simulator consumption surfaces.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/step/10-性能-安全-观测-审计治理.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This closes the first Studio simulator UI expansion slice of `CP10-3`; the shared governance regression report now covers execution, evidence-store, evidence-viewer, and simulator UI contracts in one repeatable artifact.
- The report still stays contract-first: it widens by reusing existing simulator UI contracts, not by creating a second Studio-only governance stack.
- The next optimal expansion is the next remaining governed tool family, not a new report format.
