## Highlights

- Extends the Step 10 governance regression report into shared SDK git-source materialization and VitePress toolchain contracts.
- Adds explicit contract coverage for `scripts/prepare-shared-sdk-git-sources.mjs` and `scripts/run-vitepress.mjs`, then reuses those tests inside `scripts/governance-regression-report.mjs`.
- Raises the shared JSON governance regression artifact from 60 to 62 checks while bringing SDK git-source materialization and docs build fallback orchestration into the same repeatable report as runtime, delivery, browser, workspace-quality, release-asset, release-note, smoke, architecture, release-entry, and build-toolchain governance.

## Scope

- `scripts/run-vitepress.mjs`
- `scripts/run-vitepress.test.mjs`
- `scripts/prepare-shared-sdk-git-sources.test.mjs`
- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/run-vitepress.test.mjs`
- `node scripts/prepare-shared-sdk-git-sources.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next Step 10 docs/build-toolchain slice by making BirdCoder's docs wrapper and shared SDK git-source wrapper visible in the machine-readable governance artifact instead of leaving them as standalone local utility entrypoints only.
- The report stays contract-first: it widens by importing the new wrapper tests instead of widening the report schema.
- The next best expansion remains another uncovered operational contract family with direct developer-experience or delivery risk, not duplicate reporting surfaces.
