## Highlights

- Removes the final legacy `AI CLI profile` fallback fragment from `TerminalPage`, so active terminal launch copy is now fully owned by the shared launch-presentation model.
- Converts the dead-code cleanup into an explicit contract check, preventing the old AI CLI placeholder text from slipping back into the render path.
- Keeps blocked AI CLI guidance aligned with Settings-based environment configuration messaging.
- Updates Step 08 and architecture standards so terminal launch rendering must stay free of unused fallback JSX.

## Scope

- `packages/sdkwork-birdcoder-terminal/src/pages/TerminalPage.tsx`
- `scripts/terminal-cli-registry-contract.test.ts`
- `docs/架构/16-终端主机会话-运行配置-本地存储标准.md`
- `docs/step/08-terminal主机会话-cli集成-sqlite标准化.md`
- `docs/release/releases.json`

## Verification

- `node scripts/terminal-cli-registry-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd build`
- `pnpm.cmd docs:build`
- `pnpm.cmd check:multi-mode`

## Notes

- This closes the previously documented carry-forward item around the hidden legacy AI CLI fragment in `TerminalPage`.
- Carry forward remains richer install actions and broader propagation of the same terminal launch guidance into Code/Studio entry points.
