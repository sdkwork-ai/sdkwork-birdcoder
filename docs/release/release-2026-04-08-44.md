## Highlights

- Adds `Copy Summary` to the unified Studio evidence viewer so the current Step 07 evidence slice can move into triage or drafting flows without carrying full entry payloads.
- Introduces a shared summary builder that reuses the normalized visible-slice fields already used by the panel and export diagnostics.
- Keeps the iteration inside the Step 07 clipboard lane without widening storage, release schemas, or renderer contracts.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-summary-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- The summary output intentionally stays compact and omits full entry serialization because JSON export and diagnostics already cover the heavy triage path.
- This closes the clipboard gap between the rendered visible-slice panel summary and the standardized Step 07 builder family.
- The next bounded Step 07 slice can focus on compare-summary parity or on consolidating the growing viewer builder layer.
