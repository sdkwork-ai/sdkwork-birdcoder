## Highlights

- Removes the governance-report CLI top-level-await entry, so `node scripts/governance-regression-report.mjs` no longer deadlocks into Node exit code `13` when it executes checks that import governance metadata from the same module graph.
- Realigns the active governance docs, loop-control prompt, and architecture summaries to the current `101`-check baseline instead of the stale `88`-check narrative.
- Regenerates `artifacts/governance/governance-regression-report.json` so the current host truth is explicit and auditable as `status: blocked`, `totalChecks: 101`, `passedCount: 100`, `blockedCount: 1`, and `failedCount: 0`.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/架构/10-开发流程-质量门禁-评估标准.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/架构/28-Governance-Regression-Deterministic-Baseline-Standard.md`
- `docs/step/19-Governance-Regression-Deterministic-Baseline-Lane.md`
- `artifacts/governance/governance-regression-report.json`
- `docs/release/release-2026-04-15-01.md`
- `docs/release/releases.json`

## Verification

- `pnpm.cmd exec tsc --noEmit`
- `pnpm.cmd run check:governance-regression-contract`
- `pnpm.cmd run check:live-docs-governance-baseline`
- `pnpm.cmd run check:release-flow`
- `pnpm.cmd run check:governance-regression`

## Notes

- `pnpm.cmd run check:governance-regression` still exits non-zero on this Windows host because the command-backed `web-bundle-budget` lane cannot spawn its declared `powershell.exe` runner. That result is now the correct structured `blocked` truth, not a deadlocked CLI or stale documentation baseline.
- The active governance artifact now records the full official-SDK-first engine governance lane inside the `101`-check total, so docs and release governance no longer undercount those contracts.

## Post-release operations

- Observation goal: keep the governance-report CLI entry, active docs, and generated governance artifact aligned on the same `101`-check baseline and blocked-runner semantics.
- Observation window: after any edit to `scripts/governance-regression-report.mjs`, `scripts/governance-regression-report.test.mjs`, `scripts/live-docs-governance-baseline.test.mjs`, or the touched core/prompt/architecture docs, rerun `pnpm.cmd run check:governance-regression-contract`, `pnpm.cmd run check:live-docs-governance-baseline`, `pnpm.cmd run check:release-flow`, and `pnpm.cmd run check:governance-regression`.
- Stop-ship signals: `node scripts/governance-regression-report.mjs` returns an unsettled top-level-await failure or fails to emit its report artifact; active docs drift back to `88` checks; or `check:release-flow` reopens the live-docs governance blocker.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-15-01 --release-assets-dir artifacts/release`.
- Re-issue path: restore the non-top-level-await CLI entry and `101`-check docs truth -> rerun `pnpm.cmd exec tsc --noEmit`, `pnpm.cmd run check:governance-regression-contract`, `pnpm.cmd run check:live-docs-governance-baseline`, `pnpm.cmd run check:release-flow`, and `pnpm.cmd run check:governance-regression`.
- Writeback targets: `scripts/governance-regression-report.mjs`, `scripts/governance-regression-report.test.mjs`, `docs/core/release-and-deployment.md`, `docs/prompts/反复执行Step指令.md`, `docs/架构/10-开发流程-质量门禁-评估标准.md`, `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`, `docs/架构/14-现状基线-差距-演进路线.md`, `docs/架构/28-Governance-Regression-Deterministic-Baseline-Standard.md`, `docs/step/19-Governance-Regression-Deterministic-Baseline-Lane.md`, `artifacts/governance/governance-regression-report.json`, `docs/release/release-2026-04-15-01.md`, and `docs/release/releases.json`.
