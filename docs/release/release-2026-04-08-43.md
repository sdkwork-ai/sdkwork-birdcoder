## Highlights

- Adds `Copy Compare Diagnostics` to the unified Studio evidence viewer so two selected evidence records can move directly into automation-friendly JSON.
- Introduces a shared comparison diagnostics builder that reuses the standardized Step 07 pairwise difference dimensions and serializes both evidence entries.
- Keeps the iteration inside the Step 07 compare/clipboard lane without widening storage, release schemas, or renderer contracts.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-diagnostics补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- The comparison diagnostics bundle remains intentionally limited to exactly two selected entries so the JSON shape stays deterministic.
- This closes the machine-readable side of the pairwise compare branch after compare markdown, compare issue, and compare release-note outputs.
- The next bounded Step 07 slice can focus on consolidating the shared builder family or wiring the copied JSON into higher-level triage automation.
