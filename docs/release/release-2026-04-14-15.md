## Highlights

- Closes the remaining active-prompt stale controller drift by removing present-tense Step 12 follow-on and PostgreSQL host-pass wording that had already been superseded by later release closures.
- Extends `scripts/prompt-governance-contract.test.mjs` so the active controller must preserve the recorded `release-2026-04-13-02/03/04` history instead of restating those checkpoint states as current blockers.
- Backwrites the loop-control prompt and release registry so future autonomous loops inherit the real historical sequence: Step 12 nested-wrapper closure, PostgreSQL failure-path closure, PostgreSQL host-pass closure, and the later Step 18/20 follow-on selection.

## Scope

- `scripts/prompt-governance-contract.test.mjs`
- `docs/prompts/反复执行Step指令.md`
- `docs/release/release-2026-04-14-15.md`
- `docs/release/releases.json`

## Verification

- `node scripts/prompt-governance-contract.test.mjs`
- `node scripts/live-docs-governance-baseline.test.mjs`
- `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`
- `node scripts/check-release-closure.mjs`
- `node scripts/quality-gate-execution-report.mjs`

## Notes

- This release does not reopen Step `12`, PostgreSQL provisioning, or any Step `18/20` implementation lane. It only removes stale current-state control text from the active prompt after those checkpoints had already been closed and recorded.
- The root cause was that the prompt controller still mixed checkpoint-local notes with present-tense instructions, so later autonomous loops could be steered toward already-closed Step 12 or PostgreSQL blockers despite newer release evidence.

## Post-release operations

- Observation goal: keep the active prompt controller aligned with the recorded Step 12, PostgreSQL, Step 18, and Step 20 closure sequence instead of drifting back to stale present-tense blockers.
- Observation window: on `2026-04-14`, rerun `node scripts/prompt-governance-contract.test.mjs`, `node scripts/live-docs-governance-baseline.test.mjs`, `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`, `node scripts/check-release-closure.mjs`, and `node scripts/quality-gate-execution-report.mjs` after any edit to `docs/prompts/反复执行Step指令.md`, `scripts/prompt-governance-contract.test.mjs`, or adjacent live-doc governance files.
- Stop-ship signals: the active prompt again describes Step 12 nested-wrapper follow-ons or PostgreSQL environment availability as current blockers; prompt governance no longer rejects those stale phrases; or `check-release-closure` no longer points at this latest registry-backed note.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-14-15 --release-assets-dir artifacts/release`.
- Re-issue path: restore the prompt controller to checkpoint-local historical wording -> rerun `node scripts/prompt-governance-contract.test.mjs` -> rerun `node scripts/live-docs-governance-baseline.test.mjs` -> rerun `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs` -> rerun `node scripts/check-release-closure.mjs` -> rerun `node scripts/quality-gate-execution-report.mjs`.
- Writeback targets: `scripts/prompt-governance-contract.test.mjs`, `docs/prompts/反复执行Step指令.md`, `docs/release/release-2026-04-14-15.md`, and `docs/release/releases.json`.
