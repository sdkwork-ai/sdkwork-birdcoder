## Highlights

- Corrects the Windows runtime-runner diagnosis from an implementation-specific `powershell.exe` assumption to the real blocked capability boundary: `cmd.exe` shell execution.
- Keeps Step 12 and Step 19 evidence truthful after the runner-shape change instead of fabricating progress toward a green host when the command shell is still denied.
- Re-syncs canonical packaged runtime evidence and active docs with the current host truth so release assets, docs, and contracts no longer disagree about the blocker surface.

## Scope

- `scripts/quality-gate-execution-report.mjs`
- `scripts/quality-gate-execution-report.test.mjs`
- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `artifacts/release-openapi-canonical/quality/quality-gate-execution-report.json`
- `artifacts/release-openapi-canonical/SHA256SUMS.txt`
- `docs/core/release-and-deployment.md`
- `docs/guide/development.md`
- `docs/release/release-2026-04-15-13.md`
- `docs/release/releases.json`

## Verification

- `node scripts/quality-gate-execution-report.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `node scripts/quality-gate-execution-report.mjs`
- `node scripts/governance-regression-report.mjs`
- `node scripts/release-openapi-canonical-quality-evidence-contract.test.mjs`
- `pnpm.cmd run check:release-flow`
- `pnpm.cmd docs:build`

## Notes

- This round did not remove the host blocker. After switching both runners away from the hardcoded PowerShell wrapper, the active Windows host still blocks the underlying command shell boundary with `spawn EPERM`.
- That is an architectural improvement because the repository now reports the true missing capability instead of a narrower tool-specific symptom, which reduces the chance of making the wrong remediation decision.
- The remaining commercial-delivery blockers are still real: `quality-gate-command-runner`, `governance-regression-command-runner`, and the existing `vite-host-build-preflight` environment gate.

## Post-release operations

- Observation goal: keep Windows runner diagnostics aligned with the real blocked host capability boundary.
- Observation window: after any edit to `scripts/quality-gate-execution-report.mjs`, `scripts/governance-regression-report.mjs`, or canonical quality evidence assets, rerun the verification commands above and inspect the generated runtime/governance reports.
- Stop-ship signals: a report or canonical asset reintroduces `powershell.exe child-process execution` as the active blocker without a matching implementation change; release docs diverge from the runtime evidence JSON; or `check:release-flow` stops enforcing the updated runner contracts.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-15-13 --release-assets-dir artifacts/release-openapi-canonical`.
- Re-issue path: restore runner diagnostic alignment -> refresh canonical runtime evidence and checksums -> rerun the verification commands above.
- Writeback targets: `scripts/quality-gate-execution-report.mjs`, `scripts/quality-gate-execution-report.test.mjs`, `scripts/governance-regression-report.mjs`, `scripts/governance-regression-report.test.mjs`, `artifacts/release-openapi-canonical/quality/quality-gate-execution-report.json`, `artifacts/release-openapi-canonical/SHA256SUMS.txt`, `docs/core/release-and-deployment.md`, `docs/guide/development.md`, `docs/release/release-2026-04-15-13.md`, and `docs/release/releases.json`.
