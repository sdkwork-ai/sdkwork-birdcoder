## Highlights

- Restores governance-regression determinism by aligning architecture-boundary policy with the active runtime package topology.
- Fixes locale parity for `app.menu.previousThread` and `app.menu.nextThread` in both `en` and `zh`.
- Converts the web budget slice into a self-built check so governance regression now exposes the real remaining blocker: an oversized web bundle.

## Scope

- `scripts/check-arch-boundaries.mjs`
- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `packages/sdkwork-birdcoder-i18n/src/locales/en/app/menu.ts`
- `packages/sdkwork-birdcoder-i18n/src/locales/zh/app/menu.ts`
- `docs/reference/commands.md`
- `docs/guide/development.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/step/19-Governance-Regression-Deterministic-Baseline-Lane.md`
- `docs/架构/28-Governance-Regression-Deterministic-Baseline-Standard.md`
- `docs/release/releases.json`

## Verification

- `node scripts/check-arch-boundaries.mjs`
- `node scripts/i18n-contract.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd run build` -> blocked by real `web-bundle-budget`
- `pnpm.cmd run check:governance-regression` -> blocked only by `web-bundle-budget`

## Post-release operations

- Observation window: `0` minutes on `pending`.
- Stop-ship signals: any regression that reopens package-boundary drift, locale-key drift, or hides the web budget behind missing build artifacts; plus the current oversized asset `index-CyGeJBBo.js`.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-11-25 --release-assets-dir artifacts/release`.
- Re-issue path: reduce the web bundle, rerun `pnpm build`, rerun `pnpm check:governance-regression`, backwrite the new blocker state, then `pnpm release:finalize`.
- Writeback targets: `scripts/check-arch-boundaries.mjs`, `scripts/governance-regression-report.mjs`, `scripts/governance-regression-report.test.mjs`, `packages/sdkwork-birdcoder-i18n/src/locales/en/app/menu.ts`, `packages/sdkwork-birdcoder-i18n/src/locales/zh/app/menu.ts`, `docs/reference/commands.md`, `docs/guide/development.md`, `docs/prompts/反复执行Step指令.md`, `docs/step/19-Governance-Regression-Deterministic-Baseline-Lane.md`, `docs/架构/28-Governance-Regression-Deterministic-Baseline-Standard.md`, `docs/release/releases.json`, and `docs/release/release-2026-04-11-25.md`.

## Notes

- This release closes false-blocker cleanup only; it does not claim the web performance regression is fixed.
- The live remaining blocker is `web-bundle-budget`: `index-CyGeJBBo.js` builds at `1791.9 KiB` against the `700.0 KiB` cap.
- After the web bundle is green again, the next Step 18 target is promoting engine-governance score context into packaged release evidence and finalized smoke summaries.
