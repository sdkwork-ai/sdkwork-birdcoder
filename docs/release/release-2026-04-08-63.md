## Highlights

- Tightens `UnifiedStudioEvidenceEntry.profileId` to `TerminalProfileId`, making the normalized profile contract explicit at the viewer boundary.
- Keeps defensive `profileId` normalization inside replay, diagnostics, summary, compare, and export helpers so malformed ad-hoc entries still degrade safely to `powershell`.
- Preserves the direct normalized `profileId` consumption already used by `StudioEvidencePanel`, without weakening runtime resilience in helper-generated payloads.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration keeps the normalized-entry mainline strict while explicitly preserving runtime fallback behavior for helper inputs that may bypass unified entry assembly.
- `powershell` remains the only defensive fallback target for malformed profile input.
- The next bounded slice can continue removing redundant scope fallback logic from viewer helpers now that profile typing and panel consumption boundaries are clearer.
