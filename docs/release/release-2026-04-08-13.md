## Highlights

- Centralizes blocked-launch toast wording into the terminal runtime so TerminalPage, CodePage, and StudioPage no longer maintain their own missing-CLI guidance strings.
- Extends the Step 08 contract from shared launch state and shared blocked action to shared user-facing recovery messaging, further tightening Code/Studio/Terminal parity.
- Keeps the launch guard behavior unchanged while removing the last page-local copy drift around Codex, Claude Code, Gemini, and OpenCode recovery guidance.
- Updates Step 08 and architecture docs so shared blocked-launch messaging is now part of the formal terminal standard.

## Scope

- `packages/sdkwork-birdcoder-commons/src/terminal/runtime.ts`
- `packages/sdkwork-birdcoder-terminal/src/pages/TerminalPage.tsx`
- `packages/sdkwork-birdcoder-code/src/pages/CodePage.tsx`
- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/terminal-cli-registry-contract.test.ts`
- `docs/架构/16-终端主机会话-运行配置-本地存储标准.md`
- `docs/step/08-terminal主机会话-cli集成-sqlite标准化.md`
- `docs/release/releases.json`

## Verification

- `node scripts/terminal-cli-registry-contract.test.ts`
- `node scripts/run-config-request-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd build`
- `pnpm.cmd docs:build`
- `pnpm.cmd check:multi-mode`

## Notes

- This iteration only centralizes blocked-launch copy. It does not yet introduce i18n-backed terminal recovery messaging; that remains an optional follow-up if Step 08 needs localization parity.
- Carry forward can now move past toast-copy cleanup and focus on the next remaining cross-mode convergence gap rather than more terminal-page-local refinements.
