## Highlights

- Closes the remaining Architecture 09 and 20 engine-model next-target drift by removing stale present-tense follow-on wording from active live docs after the engine/model lane had already closed in `release-2026-04-11-12`.
- Extends `scripts/live-docs-governance-baseline.test.mjs` so Architecture 09 and 20 must now keep that engine/model follow-on only as checkpoint-local history instead of a current next serial slice.
- Backwrites the active architecture docs, loop-control prompt, and release registry so future autonomous loops do not reopen the already-closed engine/model route and shared-facade adoption lane without fresh failing evidence.

## Scope

- `scripts/live-docs-governance-baseline.test.mjs`
- `docs/架构/09-安装-部署-发布标准.md`
- `docs/架构/20-统一Rust-Coding-Server-API-协议标准.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/release/release-2026-04-14-16.md`
- `docs/release/releases.json`

## Verification

- `node scripts/live-docs-governance-baseline.test.mjs`
- `node scripts/prompt-governance-contract.test.mjs`
- `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`
- `node scripts/check-release-closure.mjs`
- `node scripts/quality-gate-execution-report.mjs`

## Notes

- This release does not reopen Step `17` implementation work. It only removes stale active-doc next-target wording after the real `core.getEngineCapabilities` / `core.listModels` closure had already been recorded and later loops had moved on.
- The root cause was that active Architecture 09 and 20 docs still mixed an old engine/model follow-on note with newer sections in the same docs that already declared those routes and shared-facade adoption real.

## Post-release operations

- Observation goal: keep Architecture 09 and 20 aligned with the already-recorded engine/model closure truth instead of drifting back to the earlier Step 17 follow-on summary.
- Observation window: on `2026-04-14`, rerun `node scripts/live-docs-governance-baseline.test.mjs`, `node scripts/prompt-governance-contract.test.mjs`, `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`, `node scripts/check-release-closure.mjs`, and `node scripts/quality-gate-execution-report.mjs` after any edit to `docs/架构/09-安装-部署-发布标准.md`, `docs/架构/20-统一Rust-Coding-Server-API-协议标准.md`, `docs/prompts/反复执行Step指令.md`, or `scripts/live-docs-governance-baseline.test.mjs`.
- Stop-ship signals: Architecture 09 or 20 again presents engine/model route closure as the current next serial slice; live-docs governance no longer rejects that stale wording; or `check-release-closure` no longer points at this latest registry-backed note.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-14-16 --release-assets-dir artifacts/release`.
- Re-issue path: restore Architecture 09, Architecture 20, and the prompt controller to checkpoint-local historical wording -> rerun `node scripts/live-docs-governance-baseline.test.mjs` -> rerun `node scripts/prompt-governance-contract.test.mjs` -> rerun `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs` -> rerun `node scripts/check-release-closure.mjs` -> rerun `node scripts/quality-gate-execution-report.mjs`.
- Writeback targets: `scripts/live-docs-governance-baseline.test.mjs`, `docs/架构/09-安装-部署-发布标准.md`, `docs/架构/20-统一Rust-Coding-Server-API-协议标准.md`, `docs/prompts/反复执行Step指令.md`, `docs/release/release-2026-04-14-16.md`, and `docs/release/releases.json`.
