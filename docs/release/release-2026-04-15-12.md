## Highlights

- Closes the remaining release-note stop-ship blind spot by promoting packaged runtime blocked tiers and runtime blocking diagnostics into the final release-decision summary, not just the detailed quality-evidence section.
- Keeps the canonical release asset internally consistent: runtime execution evidence, rendered release notes, and `SHA256SUMS.txt` now agree on the same publish-blocking truth.
- Preserves the distinction between repository completeness and host capability limits by surfacing the active runtime blocker directly in stop-ship language instead of hiding it behind static quality diagnostics only.

## Scope

- `scripts/release/render-release-notes.mjs`
- `scripts/release/render-release-notes.test.mjs`
- `scripts/release-openapi-canonical-quality-evidence-contract.test.mjs`
- `artifacts/release-openapi-canonical/release-notes.md`
- `artifacts/release-openapi-canonical/SHA256SUMS.txt`
- `docs/core/release-and-deployment.md`
- `docs/reference/commands.md`
- `docs/release/release-2026-04-15-12.md`
- `docs/release/releases.json`

## Verification

- `node scripts/release/render-release-notes.test.mjs`
- `node scripts/release-openapi-canonical-quality-evidence-contract.test.mjs`
- `node scripts/release/smoke-finalized-release-assets.test.mjs`
- `node scripts/release/render-release-notes.mjs --release-tag release-local --release-assets-dir artifacts/release-openapi-canonical --output artifacts/release-openapi-canonical/release-notes.md`
- `pnpm.cmd run check:release-flow`
- `pnpm.cmd docs:build`

## Notes

- Before this round, release notes already showed runtime execution evidence in the detail section, but the operator-facing `Stop-ship signals` line still ignored that runtime blocker and only surfaced static matrix blockers.
- That is a commercial release-governance defect because the stop-ship summary is the line most likely to be read during approval, rollback review, and publication handoff.
- The current blocker remains environmental, not a functional repository regression: the active host still blocks command-backed child-process execution needed by the governed quality runner.

## Post-release operations

- Observation goal: keep release-decision summaries faithful to packaged runtime quality evidence.
- Observation window: after any edit to `scripts/release/render-release-notes.mjs`, packaged `qualityEvidence` shape, or canonical release-note regeneration flows, rerun the verification commands above and inspect `artifacts/release-openapi-canonical/release-notes.md`.
- Stop-ship signals: rendered notes stop surfacing runtime blocked tiers or runtime blocking diagnostics; `SHA256SUMS.txt` drifts away from the refreshed canonical `release-notes.md`; or `check:release-flow` stops enforcing the canonical stop-ship/runtime-evidence contract.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-15-12 --release-assets-dir artifacts/release-openapi-canonical`.
- Re-issue path: restore runtime stop-ship aggregation -> rerender canonical release notes -> confirm checksum refresh -> rerun the verification commands above.
- Writeback targets: `scripts/release/render-release-notes.mjs`, `scripts/release/render-release-notes.test.mjs`, `scripts/release-openapi-canonical-quality-evidence-contract.test.mjs`, `artifacts/release-openapi-canonical/release-notes.md`, `artifacts/release-openapi-canonical/SHA256SUMS.txt`, `docs/core/release-and-deployment.md`, `docs/reference/commands.md`, `docs/release/release-2026-04-15-12.md`, and `docs/release/releases.json`.
