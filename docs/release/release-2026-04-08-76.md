## Highlights

- Extends the Step 10 governance regression lane into the Studio evidence-store family.
- Reuses the existing Studio Preview / Build / Test / Simulator evidence-store contracts inside the shared governance regression report instead of introducing a separate persistence-only regression pipeline.
- Keeps `artifacts/governance/governance-regression-report.json` as the single repeatable JSON surface while widening coverage from governed execution lanes into local evidence persistence lanes.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/step/10-性能-安全-观测-审计治理.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This closes the first Studio evidence-store expansion slice of `CP10-3`; the shared governance regression report now covers Preview / Build / Test / Simulator execution plus the matching local evidence-store contracts in one repeatable artifact.
- The report still stays contract-first: it widens by reusing existing evidence-store contracts, not by creating a second Studio-only governance stack.
- The next optimal expansion remains additional governed tool families or viewer-layer contracts, not new report formats.
