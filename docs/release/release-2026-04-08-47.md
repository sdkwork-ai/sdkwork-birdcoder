## Highlights

- Aligns the visible-slice markdown family on one shared `Evidence Keys/Titles` identity block so issue-template, summary, and release-note outputs no longer drift on top-level evidence identity.
- Introduces a small visible-slice identity helper inside the Step 07 viewer contract and reuses it across visible-slice builders.
- Keeps the iteration inside `viewer.ts` and contract docs without widening UI, storage, or release schemas.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约-visible-slice-identity-block补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration changes contract alignment, not the underlying viewer interaction model.
- The issue template now exposes visible-slice evidence identity before entry-level detail sections, matching the lighter summary/release outputs.
- The next bounded slice can continue extracting shared visible-slice and compare scope builders to shrink `viewer.ts` duplication further.
