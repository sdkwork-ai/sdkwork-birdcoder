## Highlights

- Normalizes missing `projectId` and `runConfigurationId` values inside `buildUnifiedStudioEvidenceEntries(...)` so raw unified Studio evidence entries already carry `global/default`.
- Aligns unified entry scope with the same defaults already used by panel cards, diagnostics bundles, compare markdown, and export payloads.
- Keeps the change bounded to the Step 07 evidence assembly layer, contract tests, and governance docs.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration moves scope normalization one layer earlier, into unified entry assembly, without changing lane filtering, replay wiring, or export schema shape.
- The normalized values remain `global` for project scope and `default` for run configuration scope, matching the existing Step 07 diagnostics and export defaults.
- The next bounded slice can continue removing remaining raw-entry drift by lifting any other fallback-only behavior to the earliest safe assembly point.
