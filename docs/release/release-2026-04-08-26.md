## Highlights

- Adds lane filters to the unified Studio evidence viewer, so mixed Preview, Build, and Simulator history can now be narrowed to one lane without leaving the shared panel.
- Keeps filtering inside the shared viewer layer instead of forking separate lane panels, preserving the same normalized evidence contract.
- Advances Step 07 evidence UX while keeping the remaining gap focused on replay/export and Test lane standardization.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/06-编译环境-预览-模拟器-测试体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration adds lane filters only; replay/export actions are still pending.
- The filter remains viewer-local and does not alter evidence persistence or release-summary behavior.
- The next Step 07 priority stays on replay/export plus Test lane standardization.
