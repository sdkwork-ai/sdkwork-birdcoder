## Highlights

- Re-runs PostgreSQL live smoke first in the loop and keeps environment truth explicit: `blocked` with `reasonCode=missing_postgresql_dsn`.
- Closes the next non-environmental Step 17D slice by making blocked PostgreSQL smoke output operationally actionable.
- Adds deterministic DSN lookup priority, rerun command, and resolution hint fields to the blocked report, and documents the same contract in step, architecture, and loop-prompt governance docs with closure override item `45`.

## Scope

- `scripts/postgresql-live-smoke.ts`
- `scripts/postgresql-live-smoke-contract.test.ts`
- `docs/step/17D-PostgreSQL-Live-Smoke-Preflight.md`
- `docs/架构/20-统一Rust-Coding-Server-API-协议标准.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/release/release-2026-04-12-10.md`
- `docs/release/releases.json`

## Verification

- `pnpm.cmd run release:smoke:postgresql-live` -> `blocked (reasonCode=missing_postgresql_dsn)` (pre-loop)
- `node --experimental-strip-types scripts/postgresql-live-smoke-contract.test.ts`
- `node scripts/live-docs-governance-baseline.test.mjs`
- `node scripts/prompt-governance-contract.test.mjs`
- `node scripts/release-flow-contract.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd run typecheck`
- `pnpm.cmd run docs:build`
- `pnpm.cmd run check:governance-regression` -> `passed (88/88)`
- `pnpm.cmd run check:release-flow`
- `pnpm.cmd run release:smoke:postgresql-live` -> `blocked (reasonCode=missing_postgresql_dsn)` (post-loop)
- `node scripts/check-release-closure.mjs`

## Notes

- This loop does not claim DSN-backed PostgreSQL runtime closure; it only improves blocked-state operability and documents the corresponding governance contract.
- Blocked smoke output now exposes `dsnEnvPriority`, `rerunCommand`, and `resolutionHint` so operators can recover without source inspection.
- Exported constants `BIRDCODER_POSTGRESQL_DSN_ENV_PRIORITY` and `BIRDCODER_POSTGRESQL_LIVE_SMOKE_RERUN_COMMAND` freeze those details for tests and downstream governance consumers.

## Post-release operations

- Observation goal: ensure blocked PostgreSQL smoke remains actionable and contract-stable while DSN is still absent.
- Observation window: monitor the next loop on `2026-04-12` with `release:smoke:postgresql-live`, `check:release-flow`, and `check:governance-regression`.
- Stop-ship signals: missing `dsnEnvPriority/rerunCommand/resolutionHint` in blocked output, contract drift in `postgresql-live-smoke-contract`, or docs-governance mismatch with closure override item `45`.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-12-10 --release-assets-dir artifacts/release`.
- Re-issue path: `pnpm release:plan` -> restore actionable blocked-output fields and docs writeback -> rerun verification gates -> `pnpm release:finalize`.
- Writeback targets: `scripts/postgresql-live-smoke.ts`, `scripts/postgresql-live-smoke-contract.test.ts`, `docs/step/17D-PostgreSQL-Live-Smoke-Preflight.md`, `docs/架构/20-统一Rust-Coding-Server-API-协议标准.md`, `docs/prompts/反复执行Step指令.md`, `docs/release/release-2026-04-12-10.md`, and `docs/release/releases.json`.
