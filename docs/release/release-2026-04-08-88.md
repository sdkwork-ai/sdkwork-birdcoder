## Highlights

- Extends the Step 10 governance regression report into explicit release command, release profile, release smoke, release asset packaging/finalization, release-notes rendering, desktop/server/deployment smoke, and BirdCoder architecture contracts.
- Reuses the existing release and architecture contract scripts inside `scripts/governance-regression-report.mjs` instead of introducing a second release-quality or architecture-quality governance report.
- Raises the shared JSON governance regression artifact from 41 to 56 checks while bringing release-asset orchestration, release-note generation, smoke verification, and architecture governance into the same repeatable report as runtime, delivery, browser, and workspace-quality governance.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next `CP10-3` delivery-governance slice by making release command orchestration, release profile normalization, package/finalize asset generation, release-note rendering, packaged smoke verification, and architecture parity visible inside one machine-readable governance artifact.
- The report stays contract-first: it widens by importing existing release and architecture contracts instead of widening the report schema.
- The next best expansion remains another uncovered operational contract family with high user-facing risk, not duplicate reporting surfaces.
