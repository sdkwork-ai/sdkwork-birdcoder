## Highlights

- Extends Step 10 governance from frozen baseline fields into real terminal execution blocking inside `sdkwork-birdcoder-commons`.
- Classifies terminal command risk before execution, blocks dangerous commands under restricted policies, and emits traceable audit-ready blocking metadata.
- Wires `check:terminal-governance` into root `lint` so dangerous-command governance is continuously enforced.

## Scope

- `packages/sdkwork-birdcoder-commons/src/terminal/runtime.ts`
- `scripts/terminal-governance-contract.test.ts`
- `package.json`
- `docs/step/10-性能-安全-观测-审计治理.md`
- `docs/架构/08-性能-安全-可观测性标准.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/terminal-governance-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This closes the first executable blocking slice of `CP10-2`; it does not yet introduce manual approval recovery or release-lane exception handling.
- Governance remains shared-first: risk levels, approval policies, audit fields, and terminal blocking semantics now come from one contract path instead of scattered runtime rules.
- The next Step 10 slice should extend the same contract into broader tool families and richer recovery evidence, not redefine terminal semantics.
