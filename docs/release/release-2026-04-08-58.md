## Highlights

- Extends Step 07 profile fallback into `StudioEvidencePanel` card presentation so missing `profileId` values display as the default terminal profile `powershell`.
- Removes the last visible `undefined` drift in the Studio evidence viewer after viewer-side and export-side profile normalization were already aligned.
- Keeps the change bounded to panel presentation, UI contract coverage, and Step 07 governance docs.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration standardizes panel display fallback only; it does not widen evidence storage, replay wiring, or export schema.
- The normalized display value remains `powershell`, matching the terminal runtime default already used by viewer and export normalization.
- The next bounded Step 07 slice can continue trimming any remaining UI/viewer formatting drift without reopening the evidence contract.
