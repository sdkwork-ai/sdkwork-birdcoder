## Highlights

- Surfaces fine-grained diagnostics fields directly inside each Studio evidence card, so evidence inspection no longer stops at command/path/profile only.
- Exposes `evidenceKey`, project scope, and run configuration identity in the same panel that already shows summary and replay/export actions.
- Keeps the diagnostics enhancement fully local to the viewer, with no drift against existing evidence contracts or release metadata.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/studio-evidence-viewer-contract.test.ts`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- Empty project scope still folds to `global`, and missing run configuration still folds to `default`, preserving a stable diagnostics surface.
- This iteration improves card-level auditability without widening the evidence schema.
- The next Step 07 slice can keep layering higher-order triage interactions on top of the now-richer diagnostics panel.
