## Highlights

- Extends the compare markdown family with shared `Entry A/B Title` identity lines so pairwise triage outputs no longer drift on top-level title context.
- Reuses the same compare-identity helper in `viewer.ts`, which removes duplicated title assembly from compare summary and compare release-note builders.
- Keeps the iteration inside the Step 07 viewer contract and docs without widening UI, storage, or release schemas.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-identity-block补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-issue-template补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-summary-template补充.md`
- `docs/架构/07-数据模型-状态模型-接口契约-compare-release-note-template补充.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This is a contract-tightening iteration: compare outputs keep the same shape, but pair identity now carries titles in the top-level block instead of splitting them across deeper sections.
- The change trims compare-family duplication while preserving the same clipboard entry points and toast flow.
- The next bounded slice can keep extracting shared scope builders inside `viewer.ts` under the same verified compare/visible-slice contract.
