## Highlights

- Extends the Step 10 governance regression report into the shared workbench preferences, chat runtime, and local store contracts.
- Reuses the existing `workbench-preferences`, `chat-runtime`, and `local-store` contracts inside `scripts/governance-regression-report.mjs` instead of introducing a second workbench or persistence governance report.
- Raises the shared JSON governance regression artifact from 24 to 27 checks while keeping the same `artifacts/governance/governance-regression-report.json` output contract.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next `CP10-3` workbench-and-persistence slice by proving that unified engine preference normalization, chat history persistence, and local store serialization live inside the same repeatable governance aggregation path as terminal and run-configuration governance.
- The report stays contract-first: it widens by importing existing workbench and persistence contracts instead of widening the report schema.
- The next best expansion remains another uncovered runtime or release-facing contract with direct operational value.
