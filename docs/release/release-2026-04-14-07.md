## Highlights

- Closes the remaining top-level architecture README coding-server maturity drift by removing the stale “minimal host skeleton” summary from the active architecture index.
- Extends `scripts/live-docs-governance-baseline.test.mjs` so the architecture README must stay aligned with the already-closed representative `core / app / admin` routes, canonical OpenAPI release evidence, and PostgreSQL host-pass truth.
- Backwrites the Step 09 historical addendum, loop-control prompt, and release registry so future autonomous loops do not reopen this earlier README placeholder summary without fresh failing evidence.

## Scope

- `scripts/live-docs-governance-baseline.test.mjs`
- `docs/架构/README.md`
- `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`
- `docs/prompts/反复执行Step指令.md`
- `docs/release/release-2026-04-14-07.md`
- `docs/release/releases.json`

## Verification

- `node scripts/live-docs-governance-baseline.test.mjs`
- `node scripts/prompt-governance-contract.test.mjs`
- `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`
- `node scripts/check-release-closure.mjs`
- `node scripts/quality-gate-execution-report.mjs`

## Notes

- This release does not reopen Step `09`, Step `17`, or Step `18` implementation work; it only removes stale top-level architecture summary language after those later route, OpenAPI, and PostgreSQL host-pass closures were already completed elsewhere in the repo.
- The root cause was an outdated README summary that still presented `coding-server` maturity as future work, even though the active architecture and Step docs had already been realigned in the previous loops.

## Post-release operations

- Observation goal: keep the top-level architecture README aligned with the later coding-server route, OpenAPI, and PostgreSQL host-pass truth instead of drifting back to the earlier minimal-host summary.
- Observation window: on `2026-04-14`, rerun `node scripts/live-docs-governance-baseline.test.mjs`, `node scripts/prompt-governance-contract.test.mjs`, `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`, `node scripts/check-release-closure.mjs`, and `node scripts/quality-gate-execution-report.mjs` after any edit to `docs/架构/README.md`, `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`, `docs/prompts/反复执行Step指令.md`, or `scripts/live-docs-governance-baseline.test.mjs`.
- Stop-ship signals: the architecture README again describes Rust host as a minimal host skeleton; it again treats representative `core / app / admin` routes or canonical OpenAPI release evidence as future work; Step 09 stops carrying the README alignment addendum; or `check-release-closure` no longer points at this latest registry-backed note.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-14-07 --release-assets-dir artifacts/release`.
- Re-issue path: restore the architecture README and Step 09 addendum to the governed host-pass maturity truth -> rerun `node scripts/live-docs-governance-baseline.test.mjs` -> rerun `node scripts/prompt-governance-contract.test.mjs` -> rerun `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs` -> rerun `node scripts/check-release-closure.mjs` -> rerun `node scripts/quality-gate-execution-report.mjs`.
- Writeback targets: `scripts/live-docs-governance-baseline.test.mjs`, `docs/架构/README.md`, `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`, `docs/prompts/反复执行Step指令.md`, `docs/release/release-2026-04-14-07.md`, and `docs/release/releases.json`.
