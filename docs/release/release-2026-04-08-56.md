## Highlights

- Extends viewer-side profile fallback into export bundle `entries` so exported raw evidence rows stay aligned with normalized summary and diagnostics output.
- Keeps the change inside `viewer.ts` and the Step 07 contract docs without widening UI, storage, or release schemas.
- Removes the last known `profileId` drift between exported raw entry payloads and the rest of the viewer-generated triage surface.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration standardizes export-entry fallback behavior; it does not change UI actions or release-flow wiring.
- The normalized profile remains `powershell`, matching the viewer-side fallback introduced in `release-2026-04-08-55`.
- The next bounded slice can continue with export-entry normalization for any remaining optional fields or return to compare-family section formatting cleanup.
