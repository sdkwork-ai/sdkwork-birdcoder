## Highlights

- Adds `Copy Issue Template` to the unified Studio evidence viewer so the current filtered evidence slice can move into issue or release triage as markdown instead of raw JSON only.
- Introduces a shared issue-template builder on top of the existing Step 07 summary and visible entry contracts, keeping triage markdown generation centralized.
- Keeps the iteration local to viewer/page interaction and does not widen release metadata or evidence storage schemas.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `packages/sdkwork-birdcoder-studio/src/evidence/StudioEvidencePanel.tsx`
- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- The issue template intentionally reuses the same visible-slice summary already used by export and panel diagnostics.
- This iteration makes the current slice easier to move into issue workflows without replacing the JSON diagnostics path.
- The next Step 07 slice can focus on batch compare/selection or deeper automation on top of the new template builder.
