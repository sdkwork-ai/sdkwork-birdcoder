## Highlights

- Closes the first Step 18 source-mirror-truth slice by aligning `workbench/kernel.ts` engine source metadata with the real `external/` mirrors for `codex`, `claude-code`, `gemini`, and `opencode`.
- Adds executable contracts for engine-kernel source truth and mirror drift instead of leaving source status correctness to prose only.
- Reconfirms that PostgreSQL live smoke remains environment-blocked as `missing_postgresql_dsn`, then moves the next non-environmental loop focus to deeper Step 18 engine-truth convergence.

## Scope

- `packages/sdkwork-birdcoder-commons/src/workbench/kernel.ts`
- `scripts/engine-kernel-contract.test.ts`
- `scripts/engine-source-mirror-contract.test.ts`
- `package.json`
- `docs/prompts/反复执行Step指令.md`
- `docs/step/18A-Engine-Source-Mirror-Truth-Lane.md`
- `docs/架构/22-多Code-Engine源码镜像真相补充标准.md`
- `docs/release/releases.json`

## Verification

- `pnpm.cmd run release:smoke:postgresql-live`
- `node --experimental-strip-types scripts/engine-source-mirror-contract.test.ts`
- `node --experimental-strip-types scripts/engine-kernel-contract.test.ts`
- `pnpm.cmd run test:engine-runtime-adapter`
- `pnpm.cmd run test:engine-conformance`
- `pnpm.cmd run test:tool-protocol-contract`
- `pnpm.cmd run test:engine-resume-recovery-contract`
- `pnpm.cmd run docs:build`
- `pnpm.cmd run check:release-flow`

## Post-release operations

- Observation window: `0` minutes on `pending`.
- Stop-ship signals: any mirrored engine regressing to placeholder source truth, `engine-kernel-contract` or `engine-source-mirror-contract` failing in release-flow, or Step 18 metadata diverging from the actual `external/` mirror state.
- Rollback entry: `pnpm release:rollback:plan -- --release-tag release-2026-04-11-19 --release-assets-dir artifacts/release`.
- Re-issue path: `pnpm release:plan` -> engine source-truth lane repair -> docs/release backwrite -> `pnpm release:finalize`.
- Writeback targets: `packages/sdkwork-birdcoder-commons/src/workbench/kernel.ts`, `scripts/engine-kernel-contract.test.ts`, `scripts/engine-source-mirror-contract.test.ts`, `package.json`, `docs/prompts/反复执行Step指令.md`, `docs/step/18A-Engine-Source-Mirror-Truth-Lane.md`, `docs/架构/22-多Code-Engine源码镜像真相补充标准.md`, `docs/release/releases.json`, and `docs/release/release-2026-04-11-19.md`.

## Notes

- PostgreSQL live smoke returned `blocked` with `reasonCode=missing_postgresql_dsn`; this release does not claim live PostgreSQL closure.
- `claude-code` and `opencode` source truth must no longer regress to `sdk-only`, `extension`, or fragment-only truth while their repository mirrors remain present under `external/`.
- The next non-environmental serial slice is pushing the same shared engine descriptor and model-catalog truth deeper into `coding-server` so server-side engine catalog behavior stops depending on duplicated local definitions.
