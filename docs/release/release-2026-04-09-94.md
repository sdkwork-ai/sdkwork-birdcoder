## Highlights

- Extends the Step 10 governance regression report into release studio evidence archive and terminal governance evidence archive contracts.
- Adds explicit contract coverage for the release evidence-archive helpers that normalize preview/build/simulator/test summaries and terminal governance summaries before finalize and finalized-smoke stages consume them.
- Raises the shared JSON governance regression artifact from 67 to 69 checks while bringing direct release-evidence archive reading and summary normalization into the same repeatable report as runtime, delivery, browser, workspace-quality, release-asset, release-note, smoke, architecture, release-entry, docs-toolchain, and build-toolchain governance.

## Scope

- `scripts/release/studio-evidence-archives.test.mjs`
- `scripts/release/terminal-governance-evidence-archive.test.mjs`
- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `package.json`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/release/studio-evidence-archives.test.mjs`
- `node scripts/release/terminal-governance-evidence-archive.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:release-flow`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next Step 10 release-evidence slice by making the archive readers and summary normalizers visible in the machine-readable governance artifact instead of only verifying them indirectly through finalize and smoke aggregators.
- The report stays contract-first: it widens by importing the new helper tests instead of widening the report schema.
- The next best expansion remains another uncovered operational contract family with direct delivery, packaging, or developer-experience risk.
