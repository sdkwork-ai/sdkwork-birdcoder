## Highlights

- Standardizes Desktop and Server host runtime descriptors around `createBirdHostDescriptorFromDistribution(...)`.
- Fixes `resolveDesktopRuntime('cn')` so the desktop runtime now honors the regional manifest `appId` and `apiBaseUrl`.
- Adds `resolveServerRuntime()` with the same core host fields plus `host`, `port`, and `configFileName`.
- Promotes `scripts/host-runtime-contract.test.ts` into the release-flow and architecture gates so runtime identity drift fails the repo pipeline.

## Scope

- `packages/sdkwork-birdcoder-host-core/src/index.ts`
- `packages/sdkwork-birdcoder-desktop/src/desktop/resolveDesktopRuntime.ts`
- `packages/sdkwork-birdcoder-desktop/src/index.ts`
- `packages/sdkwork-birdcoder-distribution/src/index.ts`
- `packages/sdkwork-birdcoder-server/src/index.ts`
- `scripts/host-runtime-contract.test.ts`
- `scripts/release-flow-contract.test.mjs`
- `scripts/sdkwork-birdcoder-architecture-contract.test.mjs`
- `package.json`
- `docs/step/09-server-runtime-openapi-桌面-服务双模落地.md`
- `docs/架构/09-安装-部署-发布标准.md`
- `docs/release/releases.json`

## Verification

- `node scripts/host-runtime-contract.test.ts`
- `node scripts/release-flow-contract.test.mjs`
- `node scripts/sdkwork-birdcoder-architecture-contract.test.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd build`
- `pnpm.cmd docs:build`
- `pnpm.cmd check:multi-mode`

## Notes

- Server keeps a local distribution-aligned map for now because the current architecture boundary only allows `sdkwork-birdcoder-server` to depend on `sdkwork-birdcoder-host-core`.
- This iteration closes the runtime descriptor parity gap; broader Step 09 work can now continue on OpenAPI and deployment convergence without redefining host identity rules.
