## Highlights

- Normalizes missing `profileId` values inside `buildUnifiedStudioEvidenceEntries(...)` so raw unified Studio evidence entries already carry the default terminal profile `powershell`.
- Aligns the `UnifiedStudioEvidenceEntry.profileId` contract with its actual runtime shape instead of relying on downstream replay, export, diagnostics, or panel fallback only.
- Keeps the change bounded to the Step 07 evidence assembly layer, contract tests, and governance docs.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration moves profile normalization one layer earlier, into unified entry assembly, without changing lane filtering, replay wiring, or export schema shape.
- The normalized value remains `powershell`, matching the existing Step 07 viewer, export, diagnostics, and panel defaults.
- The next bounded slice can continue removing any remaining contract drift by lifting other fallback rules to the earliest safe assembly point.
