## Highlights

- Fixes a real Step 07 export contract gap where the Studio evidence panel selected a lane but `StudioPage` dropped it and exported `laneFilter: all`.
- Preserves the active lane filter end to end from `StudioEvidencePanel` into `buildUnifiedStudioEvidenceExportBundle(...)`, so exported diagnostics now match the visible slice.
- Keeps the correction minimal and local to the page-level wiring, with no schema or release-flow changes.

## Scope

- `packages/sdkwork-birdcoder-studio/src/pages/StudioPage.tsx`
- `scripts/studio-evidence-viewer-ui-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- The bug lived at the panel-to-page boundary, not in the shared export bundle builder.
- This closes the mismatch between UI filtering and exported triage payloads.
- The next Step 07 slice can focus on batch comparison and issue-template automation rather than export correctness.
