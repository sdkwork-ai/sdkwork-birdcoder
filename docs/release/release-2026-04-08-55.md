## Highlights

- Aligns viewer-side profile fallback so replay, diagnostics, summaries, issue templates, and compare markdown outputs normalize missing `profileId` values onto the default terminal profile `powershell`.
- Keeps the change inside `viewer.ts` and the Step 07 contract docs without widening UI, storage, or release schemas.
- Removes a remaining source of `undefined` profile drift between evidence stores and viewer-generated triage artifacts.

## Scope

- `packages/sdkwork-birdcoder-studio/src/evidence/viewer.ts`
- `scripts/studio-evidence-viewer-contract.test.ts`
- `docs/step/07-studio视图-预览-模拟器-编译环境体系.md`
- `docs/架构/07-数据模型-状态模型-接口契约.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/studio-evidence-viewer-contract.test.ts`
- `node scripts/studio-evidence-viewer-ui-contract.test.ts`
- `node scripts/check-release-closure.mjs`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This iteration standardizes viewer-side fallback behavior; it does not change UI actions or release-flow wiring.
- The normalized profile is `powershell`, matching the existing evidence-store and terminal runtime defaults in the repo.
- The next bounded slice can continue trimming compare-family duplication or start tightening export-entry normalization under the same evidence contract.
