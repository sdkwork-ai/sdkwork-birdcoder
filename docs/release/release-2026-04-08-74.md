## Highlights

- Extends the Step 10 governance regression lane from base host runtime into host-studio preview and simulator modes.
- Reuses the existing host-studio preview and simulator contracts inside the shared governance regression report instead of creating separate Studio-host regressions.
- Keeps `artifacts/governance/governance-regression-report.json` as the single repeatable JSON surface while widening coverage to governed Studio host modes.

## Scope

- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/step/10-性能-安全-观测-审计治理.md`
- `docs/架构/12-统一工具协议-权限沙箱-审计标准.md`
- `docs/架构/14-现状基线-差距-演进路线.md`
- `docs/release/releases.json`

## Verification

- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`
- `pnpm.cmd -s exec tsc --noEmit`
- `pnpm.cmd run lint`
- `pnpm.cmd docs:build`

## Notes

- This closes the first host-studio expansion slice of `CP10-3`; the shared governance regression report now covers host runtime plus host-studio preview/simulator in the same repeatable artifact.
- The report still stays contract-first: it widens by reusing existing host-studio contracts, not by inventing a parallel Studio regression model.
- The next optimal expansion is broader tool-family coverage, not more report variants.
