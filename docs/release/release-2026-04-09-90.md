## Highlights

- Extends the Step 10 governance regression report into shared SDK package preparation and BirdCoder-native server build orchestration contracts.
- Adds explicit contract coverage for `scripts/prepare-shared-sdk-packages.mjs` and `scripts/run-birdcoder-server-build.mjs`, then reuses those tests inside `scripts/governance-regression-report.mjs`.
- Raises the shared JSON governance regression artifact from 58 to 60 checks while bringing local build toolchain preparation and BirdCoder-native server build planning into the same repeatable report as runtime, delivery, browser, workspace-quality, release-asset, release-note, smoke, architecture, and release-entry governance.

## Scope

- `scripts/run-birdcoder-server-build.mjs`
- `scripts/run-birdcoder-server-build.test.mjs`
- `scripts/prepare-shared-sdk-packages.test.mjs`
- `scripts/governance-regression-report.mjs`
- `scripts/governance-regression-report.test.mjs`
- `docs/core/release-and-deployment.md`
- `docs/release/releases.json`

## Verification

- `node scripts/run-birdcoder-server-build.test.mjs`
- `node scripts/prepare-shared-sdk-packages.test.mjs`
- `node scripts/governance-regression-report.test.mjs`
- `pnpm.cmd check:governance-regression`

## Notes

- This closes the next Step 10 build-toolchain slice by making BirdCoder's shared SDK preparation wrapper and native server build wrapper visible in the machine-readable governance artifact instead of leaving them as standalone local entrypoints only.
- The report stays contract-first: it widens by importing the new build-toolchain tests instead of widening the report schema.
- The next best expansion remains another uncovered operational contract family with direct delivery or developer-experience risk, not duplicate reporting surfaces.
