# Standalone Gateway Source Configuration

This deployable host inherits the Birdcoder deployment matrix through
`sdkwork.deployment.config.json`. Environment variables and CLI values are overrides only.
