# sdkwork-birdcoder 存在问题列表

**检查范围**: `spring-ai-plus-business/apps/sdkwork-birdcoder`  
**检查日期**: 2026-05-05  
**项目类型**: TypeScript/React Monorepo (pnpm workspace) + Rust (src-host/) + Tauri Desktop

---

## 严重级别说明

| 级别 | 说明 |
|------|------|
| 🔴 **严重** | 可能导致运行时崩溃、安全漏洞、数据丢失 |
| 🟡 **中等** | 逻辑错误、潜在的运行时异常、性能问题、功能不完整 |
| 🟢 **轻微** | 代码质量问题、边界情况处理不足、架构缺陷 |

---

# 第一部分：代码 Bug 问题

## 1. Auth 认证模块

### 🔴 严重: 硬编码开发凭据（已修复 ✅）

**文件**: [auth-surface.ts:25-26](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-auth/src/auth-surface.ts#L25-L26)

```typescript
const DEFAULT_BIRDCODER_LOCAL_DEV_ACCOUNT = "local-default@sdkwork-birdcoder.local";
const DEFAULT_BIRDCODER_LOCAL_DEV_PASSWORD = "dev123456";
```

**问题**: 开发环境的默认账号和密码硬编码在源代码中。虽然 `resolveBirdCoderAuthDevelopmentPrefill` 限制了仅在开发模式且 `builtin-local` 模式下启用，但这些凭据随源码一同分发，存在安全隐患。

**修复**: 已移除内置默认账号和密码字面量，开发预填充仅读取显式配置的 `VITE_BIRDCODER_AUTH_DEV_*` 环境变量。

**状态**: ✅ 已修复

---

### 🔴 严重: `mapBirdCoderAuthUser` 缺少空值防护（已修复 ✅）

**文件**: [auth-surface.ts:151-159](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-auth/src/auth-surface.ts#L151-L159)

```typescript
export function mapBirdCoderAuthUser(user: User): SdkworkAuthUser {
  return createSdkworkAuthUserFromCanonicalIdentity({
    avatarUrl: user.avatarUrl,
    email: user.email.trim(),          // user.email 可能为 null/undefined
    id: user.id || user.email.trim(),  // user.email.trim() 可能抛出异常
    name: user.name?.trim() || user.email.trim(),
    username: user.email.trim(),
  });
}
```

**问题**: 当 `user.email` 为 `null` 或 `undefined` 时，调用 `.trim()` 会直接抛出 TypeError。

**修复**: 已改为 `user.email?.trim() ?? ''`

**状态**: ✅ 已修复

---

### 🔴 严重: `createBirdCoderAuthSession` 缺少空值防护（已修复 ✅）

**文件**: [auth-surface.ts:162-167](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-auth/src/auth-surface.ts#L162-L167)

**问题**: `user.email.trim()` 无空值保护。

**修复**: 已添加 `user.email?.trim() ?? ''`

**状态**: ✅ 已修复

---

### 🔴 严重: `resolveSyntheticSessionKey` 缺少空值防护（已修复 ✅）

**文件**: [auth-surface.ts](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-auth/src/auth-surface.ts)

**问题**: `user.email.trim()` 在 controller 的 `resolveSyntheticSessionKey` 中无空值保护。

**修复**: 已添加 `user.email?.trim() ?? ''`

**状态**: ✅ 已修复

---

### 🔴 严重: `mapBirdCoderQrAuthenticatedUser` 缺少空值防护（已修复 ✅）

**文件**: [AuthPage.tsx:39-51](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-auth/src/pages/AuthPage.tsx#L39-L51)

```typescript
function mapBirdCoderQrAuthenticatedUser(user: SdkworkAuthUser): User {
  const email = user.email.trim();         // 可能为 null/undefined
  const displayName = user.displayName.trim();  // 可能为 null/undefined
```

**问题**: `user.email` 和 `user.displayName` 直接调用 `.trim()` 无空值保护。

**修复**: 已改为 `user.email?.trim() ?? ''` 和 `user.displayName?.trim() ?? ''`

**状态**: ✅ 已修复

---

## 2. Chat 聊天引擎模块

### 🔴 严重: `getRuntimeProcess` 未处理 `globalThis.process` 不存在的情况（经复核问题不成立）

**文件**: [providerAdapter.ts:203-208](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat/src/providerAdapter.ts#L203-L208)

```typescript
function getRuntimeProcess(): NodeRuntimeProcess | null {
  const runtime = globalThis as typeof globalThis & {
    process?: NodeRuntimeProcess;
  };
  return runtime.process ?? null;
}
```

**复核**: 已有 `?? null` 空值保护，问题不成立。

**状态**: ❌ 问题不成立，无需修复

---

### 🔴 严重: `stringifyTranscriptPayload` 可能因循环引用而崩溃（经复核问题不成立）

**文件**: [providerAdapter.ts:311-325](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat/src/providerAdapter.ts#L311-L325)

```typescript
function stringifyTranscriptPayload(value: unknown): string | null {
  // ...
  try {
    const serializedValue = stringifyBirdCoderApiJson(value);
    return serializedValue && serializedValue !== 'null' ? serializedValue : null;
  } catch {
    return null;
  }
}
```

**复核**: 已有 try-catch 保护，问题不成立。

**状态**: ❌ 问题不成立，无需修复

---

### 🟡 中等: `executeClaudeCliTurn` 中 AbortSignal 使用了 `once` + `removeEventListener`（已修复 ✅）

**文件**: [chat-claude/index.ts:562-599](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-claude/src/index.ts#L562-L599)

```typescript
abortSignal?.addEventListener('abort', handleAbort, { once: true });
// ...
abortSignal?.removeEventListener('abort', handleAbort);
```

**问题**: 使用 `{ once: true }` 后监听器自动移除，`finally` 块中的 `removeEventListener` 是冗余代码。

**修复**: Claude CLI fallback 的 abort 监听改为普通监听器，并在 `finally` 中统一移除；Codex CLI fallback 也保持同一清理模式。

**状态**: ✅ 已修复

---

### 🟡 中等: `streamResponseAsChunks` 回退实现是假流式（已修复 ✅）

**文件**: [chat/index.ts:459-498](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat/src/index.ts#L459-L498)

**问题**: 将非流式 CLI 调用包装成 `streamResponseAsChunks` 会将整个响应塞入单个 chunk 再拆分，丧失了真正的流式体验。

**修复**: Claude、Codex、Gemini、OpenCode 默认 CLI fallback 均改为真实 JSONL/event streamer，`sendMessageStream` 直接消费 CLI stdout 事件并即时产出 chunk。`streamResponseAsChunks` 仅保留给不具备真实 stream API 的 sendMessage-only SDK bridge 或显式注入的自定义非流式 fallback 作为兼容降级。

**状态**: ✅ 已修复

---

## 3. Code 代码页面模块

### 🟡 中等: `createCodeQuickOpenSearchTask` 使用非标准 API（经复核问题不成立）

**文件**: [codeFileSearch.ts:112-134](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-code/src/pages/codeFileSearch.ts#L112-L134)

**复核**: `scheduleNextQuickOpenSearchChunk` 和 `cancel` 函数中已有 `typeof window.requestIdleCallback === 'function'` 和 `typeof window.cancelIdleCallback === 'function'` 的兼容性检测，且有 `setTimeout` 回退。

**状态**: ❌ 问题不成立，无需修复

---

### 🟡 中等: `createCodeQuickOpenSearchTask` 搜索栈深度无上限（已修复 ✅）

**文件**: [codeFileSearch.ts:86-92](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-code/src/pages/codeFileSearch.ts#L86-L92)

```typescript
if (node.children?.length) {
    searchStack.push({
      currentPath: nextPath,
      index: 0,
      nodes: node.children,
    });
  }
```

**问题**: 深层嵌套的文件夹结构会在 `searchStack` 中无限增长，极端情况下可能导致内存压力。

**修复**: 已添加 `CODE_QUICK_OPEN_SEARCH_MAX_STACK_DEPTH` 并在 push 子节点搜索帧前检查上限，避免深层树导致搜索栈无界增长。

**状态**: ✅ 已修复

---

## 4. 用户中心模块

### 🟡 中等: `readBirdCoderUserProfile` 和 `writeBirdCoderUserProfile` 静默失败（已修复 ✅）

**文件**: [storage.ts:56-88](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-user/src/storage.ts#L56-L88)

**问题**: 如果 `runtimeClient` 为 null/falsy，直接读写本地存储作为后备，但没有区分"客户端未就绪"和"网络错误"。在多端同步场景下可能造成数据不一致。

**修复**: 本地 fallback 快照现在显式标记 `syncMode: 'local-only'` 和 `syncReason`，调用方可以区分远端 runtime 未绑定与正常远端同步结果。

**状态**: ✅ 已修复

---

### 🟡 中等: `createBirdCoderUserRouteIntent` 中 section 参数使用不一致（已修复 ✅）

**文件**: [user.ts:71-83](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-user/src/user.ts#L71-L83)

**问题**: `sectionId` 被重复使用：既用于创建 section route intent，也在 [vip.ts:162](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-user/src/vip.ts#L162) 中作为 URL query parameter 传递，两种用法不一致。

**修复**: VIP route intent 改为规范 `section` 参数，并移除废弃兼容字段 `sectionId`；输出统一使用 canonical `section` query parameter 和 intent 字段。

**状态**: ✅ 已修复

---

## 5. 存储与持久化模块

### 🟡 中等: `draftStore.ts` 中的 Map 可能无限增长（已修复 ✅）

**文件**: [draftStore.ts:5-6](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-commons/src/chat/draftStore.ts#L5-L6)

```typescript
const chatInputDrafts = new Map<string, string>();
const chatInputDraftListeners = new Map<string, Set<ChatInputDraftListener>>();
```

**问题**: 全局 Map 持有的草稿数据不会自动清理。长时间运行的会话中，用户切换不同 project/session 会产生大量 key，内存持续增长。

**修复**: 已添加 `MAX_CHAT_INPUT_DRAFT_ENTRIES` 和 `pruneChatInputDrafts`，按最近使用顺序清理旧草稿，避免全局 Map 无界增长。

**状态**: ✅ 已修复

---

### 🟡 中等: `setWorkbenchChatInputDraft` 中 falsy 值处理（经复核问题不成立）

**文件**: [draftStore.ts:82-86](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-commons/src/chat/draftStore.ts#L82-L86)

**复核**: `"0"` 和 `"false"` 字符串在 JS 中是 truthy 值，`if (resolvedValue)` 仅对空字符串 `""` 返回 false，行为正确。

**状态**: ❌ 问题不成立，无需修复

---

## 6. 终端运行时模块

### 🟡 中等: `listTerminalCliProfileAvailability` 并发调用无保护（已修复 ✅）

**文件**: [runtime.ts:344-380](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-commons/src/terminal/runtime.ts#L344-L380)

**问题**: 对每个 CLI profile 同时发起 `invoke` 调用，在 Rust 后端可能引发并发竞态或性能问题。

**修复**: 已添加 `TERMINAL_CLI_PROFILE_AVAILABILITY_CONCURRENCY` 和 `mapWithConcurrencyLimit`，终端 profile 可用性检测通过有界并发调度。

**状态**: ✅ 已修复

---

## 7. Scripts 脚本模块

### 🟡 中等: `replace-colors.cjs` 同步覆盖文件，无备份（已修复 ✅）

**文件**: [replace-colors.cjs:45-47](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/replace-colors.cjs#L45-L47)

```javascript
if (content !== originalContent) {
    fs.writeFileSync(filePath, content, 'utf8');
```

**问题**: 直接同步覆盖源文件，如果正则替换出错，会导致代码被错误修改且无法恢复。

**修复**: 写入源文件前会先通过 `writeBackupFileSync` 生成备份文件，备份路径处理保持显式。

**状态**: ✅ 已修复

---

### 🟢 轻微: `replace-colors.cjs` 和 `replace_colors.cjs` 功能不同但与预期相反（经复核问题不成立）

**复核**: 两个文件功能相反——一个将 hex 转 Tailwind 类名，另一个将 Tailwind 类名转 hex，并非重复文件。

**状态**: ❌ 问题不成立，无需删除

---

## 8. Projection 投影存储模块

### 🟡 中等: `operationRecordToRow` 中脆弱的 session ID 提取（已修复 ✅）

**文件**: [projectionRepository.ts:296-298](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-server/src/projectionRepository.ts#L296-L298)

```typescript
coding_session_id:
    value.streamUrl?.split('/coding-sessions/')[1]?.split('/')[0] ?? '',
```

**问题**: 通过 URL 字符串分割来提取 session ID，依赖于特定的 URL 格式。如果 URL 格式发生变化（如 query parameters），会导致提取失败。

**修复**: 已改为正则 `/\/coding-sessions\/([^/?]+)/` 匹配，支持 URL query 参数等边界情况。

**状态**: ✅ 已修复

---

### 🟡 中等: `normalizeRuntimeRecord` 对 `modelId` 的非空检验过于严格（已修复 ✅）

**文件**: [projectionRepository.ts:86-92](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-server/src/projectionRepository.ts#L86-L92)

```typescript
if (!normalizedModelId) {
    return null;
}
```

**问题**: `model_id` 为空时丢弃整个运行时记录。但实际业务中可能存在 model_id 尚未确定的场景。

**修复**: 已移除空 `model_id` 直接丢弃记录的逻辑，改用 `DEFAULT_UNKNOWN_RUNTIME_MODEL_ID` 占位保留运行时记录。

**状态**: ✅ 已修复

---

## 9. Web/Desktop 启动模块

### 🟡 中等: `document.getElementById('root')!` 非空断言（已修复 ✅）

**文件**: [web/main.tsx:49](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-web/src/main.tsx#L49) / [desktop/main.tsx:54](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-desktop/src/main.tsx#L54)

```typescript
createRoot(document.getElementById('root')!).render(...)
```

**问题**: 使用 `!` 非空断言，如果 HTML 中缺少 `#root` 元素，会导致运行时 null reference 错误。

**修复**: 已改为显式 null 检查和错误抛出。

**状态**: ✅ 已修复

---

## 10. Skills 技能模块

### 🟡 中等: `installPackage` 中存在竞态条件（已修复 ✅）

**文件**: [SkillsPage.tsx:333-363](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-skills/src/SkillsPage.tsx#L333-L363)

**问题**: `installingId` 状态用于跟踪当前安装项，但未在函数开头检查是否已有安装进行中。用户可以快速点击多个安装按钮，导致并发安装请求互相干扰。

**修复**: 已在函数开头添加 `if (installingId !== null) { return; }` 守护。

**状态**: ✅ 已修复

---

# 第二部分：Code Engine 功能完整性问题

## IChatEngine 接口定义

接口定义位置: [types.ts:269-301](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat/src/types.ts#L269-L301)

| 方法 | 是否必选 | 用途 |
|---|---|---|
| `sendMessage` | 必选 | 非流式消息发送 |
| `sendMessageStream` | 必选 | 流式消息发送 |
| `initialize` | 可选 | 引擎初始化 |
| `describeRuntime` | 可选 | 运行时描述符 |
| `describeIntegration` | 可选 | 集成信息描述 |
| `getCapabilities` | 可选 | 获取能力快照 |
| `describeRawExtensions` | 可选 | 原始扩展描述 |
| `sendCanonicalEvents` | 可选 | 发送规范事件流 |
| `getHealth` | 可选 | 健康检查报告 |
| `createSession/getSession` | 可选 | 会话管理 |
| `createCodingSession/getCodingSession/addMessageToCodingSession` | 可选 | 编码会话管理 |
| `updateContext` | 可选 | 上下文更新 |
| `onToolCall` | 可选 | 工具调用回调 |

---

## 11. 四引擎实现对比矩阵

| 方法 | Claude | Codex | Gemini | OpenCode |
|---|---|---|---|---|
| `sendMessage` | ✅ | ✅ | ✅ | ✅ |
| `sendMessageStream` | ✅ | ✅ | ✅ | ✅ |
| `initialize` | ❌ | ❌ | ❌ | ❌ |
| `describeRuntime` | ✅ | ✅ | ✅ | ✅ |
| `describeIntegration` | ✅ | ✅ | ✅ | ✅ |
| `getCapabilities` | ✅ | ✅ | ✅ | ✅ |
| `describeRawExtensions` | ✅ | ✅ | ✅ | ✅ |
| `sendCanonicalEvents` | ✅ | ✅ | ✅ | ✅ |
| `getHealth` | ✅ | ✅ | ✅ | ✅ |
| `createSession/getSession` | ✅ | ✅ | ✅ | ✅ |
| `createCodingSession/getCodingSession/addMessageToCodingSession` | ✅ | ✅ | ✅ | ✅ |
| `updateContext` | ✅ | ✅ | ✅ | ✅ |
| `onToolCall` | ✅ | ✅ | ✅ | ✅ |
| **CLI fallback** | ✅ `claude` | ✅ `codex` | ✅ `gemini` | ✅ `opencode` |

---

## 12. 共同缺失的方法（4个引擎全部未实现）（已修复 ✅）

以下 7 个方法在所有引擎中均未实现：

| 缺失方法 | 影响 |
|---|---|
| `describeRuntime` | 上层无法获取引擎运行时描述符（modelId、transportKind、approvalPolicy、capabilityMatrix） |
| `getCapabilities` | 无法动态获取引擎能力快照（declaredCapabilities、detectedCapabilities、experimentalCapabilities） |
| `sendCanonicalEvents` | 规范事件流全部缺失，无法统一不同引擎的事件格式 |
| `createSession/getSession` | 无会话管理能力 |
| `createCodingSession/getCodingSession/addMessageToCodingSession` | 无编码会话管理能力 |
| `updateContext` | 运行时上下文更新不可用 |
| `onToolCall` | 工具调用无法回调 IDE 层执行 |

**修复**: 四个引擎均已实现 `describeRuntime()`、`getCapabilities()`、`sendCanonicalEvents()`、本地 session/coding-session 管理、`updateContext()` 和 `onToolCall()`，统一通过共享 provider adapter 生成 runtime descriptor、capability snapshot 和 canonical event stream。

**状态**: ✅ 已修复

---

## 13. 各引擎独立问题

### 🔴 严重: Codex 不使用共享桥接工具函数（已修复 ✅）

**文件**: [codex/index.ts:891-919](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-codex/src/index.ts#L891-L919)

```typescript
async sendMessage(messages: ChatMessage[], options?: ChatOptions): Promise<ChatResponse> {
    const bridge = await this.officialSdkBridgeLoader?.load();  // 直接加载
    if (bridge?.sendMessage) {
      return bridge.sendMessage(messages, options);
    }
    // ... 回退到 CLI
}
```

**问题**: Codex 直接调用 `this.officialSdkBridgeLoader?.load()` 而非使用共享的 `invokeWithOptionalOfficialSdk`。其他三个引擎都通过统一函数桥接解析，Codex 的独立实现意味着：
- 任何对 `invokeWithOptionalOfficialSdk` 的改进/修复都不会自动惠及 Codex
- 桥接加载的错误处理逻辑不一致
- 代码重复，维护负担增加

同样的问题存在于 `sendMessageStream` [codex/index.ts:921-1001](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-codex/src/index.ts#L921-L1001)。

**修复**: Codex `sendMessage`/`sendMessageStream` 已改用 `invokeWithOptionalOfficialSdk` 和 `streamWithOptionalOfficialSdk`，不再直接加载 bridge。

**状态**: ✅ 已修复

---

### 🔴 严重: Gemini 无 CLI 回退，SDK 不可用时直接抛异常（已修复 ✅）

**文件**: [gemini/index.ts:590-608](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-gemini/src/index.ts#L590-L608)

```typescript
getHealth() {
    return createDetectedHealthReport({
      // ...
      fallbackRuntimeMode: null,     // ← 显式禁用回退
      fallbackAvailable: false,       // ← 显式禁用回退
    });
}

// sendMessage fallback:
fallback: async () => {
    throw createUnavailableGeminiSdkError();
},
```

**问题**: Gemini 引擎硬编码 `fallbackRuntimeMode: null` 和 `fallbackAvailable: false`。如果 `@google/gemini-cli-sdk` 未安装（用户环境中常见），引擎直接抛异常完全不可用。

**修复**: 已添加 `geminiCliJsonlTurnExecutor` 和默认 spawn 版 `streamGeminiCliJsonlTurn`，健康检查暴露 `headless` fallback，`sendMessage`/`sendMessageStream` 在 SDK 不可用时走真实 Gemini CLI JSONL lane。

**状态**: ✅ 已修复

---

### 🔴 严重: OpenCode 同样无 CLI 回退（已修复 ✅）

**文件**: [opencode/index.ts:750-758](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-opencode/src/index.ts#L750-L758)

```typescript
getHealth() {
    return createDetectedHealthReport({
      // ...
      fallbackRuntimeMode: null,     // ← 同样硬编码 null
      fallbackAvailable: false,       // ← 同样硬编码 false
    });
}
```

**问题**: 与 Gemini 完全相同的问题。OpenCode 作为聚合多个 provider 的通用网关，不支持 CLI 回退是一个重大缺陷。

**修复**: 已添加 `openCodeCliJsonlTurnExecutor` 和默认 spawn 版 `streamOpenCodeCliJsonlTurn`，健康检查暴露 `headless` fallback，`sendMessage`/`sendMessageStream` 在 SDK 不可用时走真实 OpenCode CLI JSONL lane。

**状态**: ✅ 已修复

---

### 🟡 中等: Codex CLI 流式响应是假流式（已修复 ✅）

**文件**: [codex/index.ts:937-1001](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-codex/src/index.ts#L937-L1001)

```typescript
const events = await this.cliJsonlTurnExecutor(...);  // 等待全部完成

for (const event of events) {  // 然后逐条 yield
```

**问题**: CLI JSONL 模式下，`await` 等待 CLI 进程完全退出后才开始迭代。对于大响应，用户会看到长时间无反馈然后突然全部输出。

**修复**: Codex 默认 CLI fallback 已改为 `streamCodexCliJsonlTurn`，边读 stdout JSONL 边产出 stream chunk；仅自定义注入的非流式 executor 保留兼容降级。

**状态**: ✅ 已修复

---

### 🟡 中等: 所有引擎使用静态常量定义，无运行时发现机制（已修复 ✅）

**文件**: 各引擎 `describeIntegration()` 实现

**问题**: 所有引擎的 `describeIntegration()` 都返回硬编码的静态常量（如 `CLAUDE_INTEGRATION`、`CODEX_INTEGRATION`）。这意味着引擎的 transportKinds、sourceMirrorStatus 等在 launch 时就已经固定，无法动态发现新安装的 SDK 包。

**修复**: 已新增共享 `createRuntimeIntegrationDescriptor`，四个引擎的 `describeIntegration()` 均在调用时重新解析 SDK 包版本和 source mirror 状态，避免返回模块加载时冻结的静态描述符。

**状态**: ✅ 已修复

---

## 14. 架构层面问题

### 🟡 中等: 引擎注册表缺失（已修复 ✅）

**问题**: `types.ts` 定义了 `IChatEngine` 接口，但 `sdkwork-birdcoder-chat/src/index.ts` 只是一个简单的 re-export 文件，没有任何引擎注册/发现机制。四个引擎实现各自独立导出 `XxxChatEngine` 类，但没有统一的 Factory 或 Registry 来根据 `BirdCoderCodeEngineKey` 创建对应的引擎实例。

上层调用者需要手动 import 和选择引擎类，容易出错。

**修复**: `@sdkwork/birdcoder-chat` 已暴露 `ChatEngineRegistryEntry`、`ChatEngineRegistry` 和 `createChatEngineRegistry`，供上层统一注册、发现和创建引擎。

**状态**: ✅ 已修复

---

### 🟡 中等: 能力矩阵在引擎层未暴露（已修复 ✅）

**问题**: `BirdCoderEngineCapabilityMatrix` 类型在 types 中被引用但没有任何引擎实际使用它。所有引擎的 `getCapabilities()` 都未实现，`describeRuntime()` 也未实现。

**修复**: 四个引擎均通过 `createDefaultChatCanonicalRuntimeDescriptor` 和 `createDefaultChatEngineCapabilitySnapshot` 暴露 runtime descriptor 与 capability snapshot。

**状态**: ✅ 已修复

---

### 🟡 中等: 工具调用处理在各引擎 SDK 桥接中各自实现（部分治理 ✅）

**问题**: 每个引擎对 tool_calls 的解析、拆分、增量处理逻辑各自独立，缺乏共享的工具调用规范化层。

| 引擎 | 工具调用处理位置 |
|---|---|
| Claude | [createClaudeOfficialSdkBridge](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-claude/src/index.ts#L603) |
| Codex | [createCodexOfficialSdkBridge](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-codex/src/index.ts) |
| Gemini | [createGeminiOfficialSdkBridge](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-gemini/src/index.ts#L408) |
| OpenCode | [createOpenCodeOfficialSdkBridge](file:///d:/javasource/spring-ai-plus/spring-ai-plus-business/apps/sdkwork-birdcoder/packages/sdkwork-birdcoder-chat-opencode/src/index.ts#L516) |

**修复**: 已抽出并复用共享 code-engine dialect 层，包括 provider tool name canonicalization、tool argument JSON 安全序列化、tool-call delta merge/flush、runtime status/approval/user-question 语义解析。各 provider 仍保留必要的 native event shape 解析，但跨引擎语义和 canonical 输出已由共享层约束。

**状态**: ✅ 已完成共享语义治理；provider native 事件解析保持局部实现

---

# 第三部分：汇总统计

## 按模块统计

| 模块 | 🔴 严重 | 🟡 中等 | 🟢 轻微 |
|------|:---:|:---:|:---:|
| Auth 认证 | 1 (✅1) | 0 | 0 |
| Chat 聊天引擎 | 0 | 2 (✅2) | 0 |
| Code 代码页面 | 0 | 1 (✅1) | 0 |
| User 用户中心 | 0 | 2 (✅2) | 0 |
| Storage 存储 | 0 | 1 (✅1) | 0 |
| Terminal 终端 | 0 | 1 (✅1) | 0 |
| Scripts 脚本 | 0 | 1 (✅1) | 0 |
| Projection 投影 | 0 | 1 (✅1) | 0 |
| Web/Desktop 启动 | 0 | 0 | 0(已修复) |
| Skills 技能 | 0 | 0 | 0(已修复) |
| **Code Engine 功能** | **3 (✅3)** | **6 (✅6)** | **0** |
| **合计** | **4 (✅4)** | **16 (✅16)** | **0** |

> 注: ⬜ = 未修复, ✅ = 已修复；“问题不成立”项未计入待修复总数。

## 已修复问题（20个）

| # | 问题 | 文件 |
|---|------|------|
| 1 | `mapBirdCoderAuthUser` .trim() 空值 | auth-surface.ts |
| 2 | `createBirdCoderAuthSession` .trim() 空值 | auth-surface.ts |
| 3 | `resolveSyntheticSessionKey` .trim() 空值 | auth-surface.ts |
| 4 | `mapBirdCoderQrAuthenticatedUser` .trim() 空值 | AuthPage.tsx |
| 5 | 脆弱的 session ID 提取 | projectionRepository.ts |
| 6 | `installPackage` 竞态条件 | SkillsPage.tsx |
| 7 | `getElementById('root')!` 非空断言 (web) | web/main.tsx |
| 8 | `getElementById('root')!` 非空断言 (desktop) | desktop/main.tsx |
| 9 | 硬编码开发凭据 | auth-surface.ts |
| 10 | Claude/Codex CLI abort 监听清理 | chat-claude/index.ts, chat-codex/index.ts |
| 11 | 默认 CLI fallback 假流式 | chat-claude/codex/gemini/opencode |
| 12 | Quick Open 搜索栈无界增长 | codeFileSearch.ts |
| 13 | 用户中心本地 fallback 静默同步模式 | storage.ts |
| 14 | VIP section 参数规范化并移除废弃兼容字段 | vip.ts |
| 15 | Chat draft store Map 无界增长 | draftStore.ts |
| 16 | Terminal CLI profile 可用性检测无界并发 | runtime.ts |
| 17 | replace-colors 覆盖前无备份 | replace-colors.cjs |
| 18 | projection runtime 空 model_id 丢弃记录 | projectionRepository.ts |
| 19 | Code Engine 共享 bridge/fallback/registry/runtime/capability/canonical event 能力缺口 | birdcoder-chat 与四引擎 |
| 20 | 工具调用跨引擎共享语义治理 | codeEngineDialect.ts 与 provider adapters |

## 待修复问题（0个）

当前清单中列出的 20 个有效问题均已完成修复或共享语义治理；经复核不成立的问题保持标记为“问题不成立”。后续可继续深化的方向是进一步抽象各 provider native event shape 解析，但这不再阻塞当前清单的功能正确性。
