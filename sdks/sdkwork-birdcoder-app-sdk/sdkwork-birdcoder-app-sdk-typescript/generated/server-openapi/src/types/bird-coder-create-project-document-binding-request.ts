export interface BirdCoderCreateProjectDocumentBindingRequest {
  /** Stable sdkwork-documents identifier. */
  documentId: string;
  /** Lower-snake-case binding purpose. */
  bindingKind: string;
}
