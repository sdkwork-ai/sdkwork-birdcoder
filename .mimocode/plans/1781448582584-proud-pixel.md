# Plan: Fix BirdCoder Auth Screen - Dual Header & Vertical Layout

## Root Causes

### Bug 1: Two Headers
`BirdcoderApp.tsx` line 2970 renders `BirdcoderAppHeader` **unconditionally**. When `activeTab === 'auth'`, the menu items are hidden (`shouldShowWorkbenchHeaderChrome = false` at line 2673) but the header div itself (with brand + window controls) still renders. Then `AuthShell` (from `@sdkwork/birdcoder-pc-auth`, `AuthShell.tsx` line 272) renders its own header in desktop mode. Result: two headers with brand + window controls.

**sdkwork-im approach**: Auth renders inside `AuthGate` which replaces the entire app â€?no app header exists during auth.

### Bug 2: Vertical QR/Form Layout
`SdkworkAuthPageShell` (in `@sdkwork/auth-pc-react`) uses Tailwind `flex-col md:flex-row` â€?horizontal layout only activates at â‰?68px width. The deep nesting and `overflow: hidden` / `min-width: 0` chain in `BirdcoderApp > AppMainBody > content-div > AuthShell > AuthPage > SdkworkIamAuthRoutes` may constrain width below 768px.

## Fix Plan

### Fix 1: Hide `BirdcoderAppHeader` when auth tab is active
**File**: `apps/sdkwork-birdcoder-pc/packages/sdkwork-birdcoder-pc-shell/src/application/app/BirdcoderApp.tsx`

At line 2970, wrap with guard:
```tsx
{activeTab !== 'auth' && (
  <BirdcoderAppHeader ... />
)}
```

### Fix 2: Ensure auth content expands to full width
**File**: `apps/sdkwork-birdcoder-pc/src/index.css`

Verify `.sdkwork-birdcoder-auth-main` and its children have `width: 100%` and `min-width: 0` is not collapsing the auth shell. The `.sdkwork-birdcoder-auth-page` needs `width: 100%`.

### Files to Modify

1. `apps/sdkwork-birdcoder-pc/packages/sdkwork-birdcoder-pc-shell/src/application/app/BirdcoderApp.tsx` (line ~2970)
2. `apps/sdkwork-birdcoder-pc/src/index.css` (auth shell/main CSS)

### Verification
1. `pnpm run typecheck`
2. `pnpm run dev` â†?verify single header, horizontal QR+form layout
