# Plan: Fix `pnpm tauri:dev` double-path-prefix error

## Problem

`pnpm tauri:dev` fails with:
```
Unable to resolve workspace package.json at E:\sdkwork-space\sdkwork-birdcoder\apps\sdkwork-birdcoder-pc\apps\sdkwork-birdcoder-pc\packages\sdkwork-birdcoder-pc-desktop\package.json
```

The `apps/sdkwork-birdcoder-pc/` segment is doubled. All static path definitions in the execution chain use the correct single prefix — the doubling happens at runtime.

## Investigation results

Traced full execution chain:
1. Root `package.json:280` → `run-workspace-package-script.mjs apps/sdkwork-birdcoder-pc/packages/sdkwork-birdcoder-pc-desktop tauri:dev` (correct)
2. `run-workspace-package-script.mjs` → reads desktop `package.json:22` → `run-birdcoder-desktop-command.mjs` (correct)
3. `run-birdcoder-desktop-command.mjs:93` → `createWorkspacePackageScriptPlan({packageDir: 'apps/sdkwork-birdcoder-pc/packages/sdkwork-birdcoder-pc-desktop', ...})` (correct)
4. Searched entire codebase for `apps/sdkwork-birdcoder-pc/apps/sdkwork-birdcoder-pc` — **zero matches**

## Plan

### Step 1: Add debug logging to identify runtime source

Add `console.error` to `scripts/run-workspace-package-script.mjs` line 37 (before the `existsSync` check):

```js
console.error(`[DEBUG] packageDir=${packageDir}, workspaceRootDir=${workspaceRootDir}, absolutePackageDir=${absolutePackageDir}`);
```

Run `pnpm tauri:dev` and observe which call produces the doubled path.

### Step 2: Fix the root cause

Based on debug output, one of:
- **If `workspaceRootDir` is wrong**: Fix `rootDir` computation or the caller passing it
- **If `packageDir` is wrong**: Find the caller passing the doubled path
- **If `path.resolve` behaves unexpectedly**: Check for Windows-specific path issues with `shell-emulator=true`

### Step 3: Search for similar double-prefix errors

Search all non-test files for path patterns that could have been affected by the bulk fix:
- `package.json` scripts across all workspace packages
- Config files (`.mjs`, `.ts`, `.json`) that reference `apps/sdkwork-birdcoder-pc/packages/`
- Any file with `apps/*/packages/` patterns

### Step 4: Clean up stale `pnpm-workspace.yaml`

Remove line 3 (`packages/sdkwork-birdcoder-*`) since root `packages/` directory was deleted.

### Step 5: Remove debug logging

Remove the debug `console.error` added in Step 1.

## Files to modify

- `scripts/run-workspace-package-script.mjs` — add/remove debug logging, fix if root cause is here
- `scripts/run-birdcoder-desktop-command.mjs` — fix if path construction is wrong
- `pnpm-workspace.yaml` — remove stale glob
- Other files TBD based on debug output

## Verification

1. `pnpm tauri:dev` — should start without the double-path error
2. `pnpm run lint` — should still pass (no regression)
3. Search for `apps/sdkwork-birdcoder-pc/apps/sdkwork-birdcoder-pc` — should return zero matches
