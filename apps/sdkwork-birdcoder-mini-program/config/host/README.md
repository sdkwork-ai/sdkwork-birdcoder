# WeChat Host Templates

Host files are safe examples only. Real WeChat App IDs, private keys, upload credentials, and developer-local settings belong in ignored host-local files such as `project.private.config.json`.
