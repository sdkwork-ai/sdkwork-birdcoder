/**
 * Reserved structural module boundary required by the SDKWork core package
 * layout (APP_COMPOSITION_SPEC / package-subpath-exports contract).
 *
 * This module intentionally exports nothing yet: it is a package skeleton
 * boundary, not a feature stub. Adding a feature here must come with a real
 * implementation and contract tests; no empty registry factories may be
 * reintroduced.
 */
export {};
